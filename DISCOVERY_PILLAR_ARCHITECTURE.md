# 🏛️ Discovery Pillar - Архитектурная Диаграмма

## 📐 Общая структура

```
┌─────────────────────────────────────────────────────────────────┐
│                      API LAYER (Next.js)                        │
│                                                                  │
│  GET /tours     POST /tours     GET /search                     │
│  GET /tours/[id] PUT /tours/[id] DELETE /tours/[id]             │
│  POST /tours/publish  GET /tours/stats  GET /tours/reviews      │
│                                                                  │
│  GET /reviews   POST /reviews   GET /reviews/[id]               │
│  PUT /reviews/[id] DELETE /reviews/[id]                         │
│  POST /reviews/approve  POST /reviews/reject                    │
│                                                                  │
│  GET /search    GET /autocomplete  GET /recommended             │
│  GET /trending  GET /tags  GET /similar                         │
└────────────────────┬────────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────────┐
│                   SERVICE LAYER (Singletons)                    │
│                                                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐ │
│  │  TourService    │  │ ReviewService   │  │ SearchService    │ │
│  │                 │  │                 │  │                  │ │
│  │ + create()      │  │ + create()      │  │ + search()       │ │
│  │ + read()        │  │ + read()        │  │ + advancedSearch │ │
│  │ + update()      │  │ + update()      │  │ + autocomplete() │ │
│  │ + delete()      │  │ + delete()      │  │ + getRecommended │ │
│  │ + search()      │  │ + approve()     │  │ + getTrending()  │ │
│  │ + publish()     │  │ + reject()      │  │ + getSimilar()   │ │
│  │ + getStats()    │  │ + respond()     │  │ + getPopularTags │ │
│  │ + updateRating()│  │ + getStats()    │  │                  │ │
│  │                 │  │ + getOperatorRating                   │ │
│  └────────┬────────┘  └────────┬────────┘  └──────────┬───────┘ │
│           │                    │                      │          │
│           └────────────────┬───┴──────────────────────┘          │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
┌────────────────────────────▼──────────────────────────────────────┐
│              INFRASTRUCTURE LAYER (Core-Infrastructure)            │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │
│  │ DatabaseSvc  │  │  CacheSvc    │  │ MonitoringSvc             │
│  │              │  │              │  │              │             │
│  │ PostgreSQL   │  │ Redis/Memory │  │ Sentry/Logs  │             │
│  │ + query()    │  │ + get()      │  │ + log()      │             │
│  │ + execute()  │  │ + set()      │  │ + record()   │             │
│  │              │  │ + delete()   │  │              │             │
│  └──────────────┘  └──────────────┘  └──────────────┘             │
│                                                                    │
│  ┌──────────────┐  ┌──────────────┐                              │
│  │ EventBusSvc  │  │ NotificationsSvc                            │
│  │              │  │              │                              │
│  │ Pub/Sub      │  │ Email/SMS    │                              │
│  │ + publish()  │  │ + send()     │                              │
│  │ + subscribe()│  │ + queue()    │                              │
│  │              │  │              │                              │
│  └──────────────┘  └──────────────┘                              │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────▼──────────────────────────────────────┐
│                  DATA LAYER (PostgreSQL)                           │
│                                                                    │
│  Table: tours                                                     │
│  ├─ id, title, description, activity, difficulty                 │
│  ├─ price_from, price_to, duration                               │
│  ├─ operator_id, rating, review_count, view_count                │
│  ├─ status (draft/published), is_active                          │
│  └─ created_at, updated_at, published_at                         │
│                                                                    │
│  Table: reviews                                                   │
│  ├─ id, tour_id, user_id, rating, title, comment                 │
│  ├─ status (pending/approved/rejected)                            │
│  ├─ helpful_count, helpful_count                                  │
│  ├─ response_from_operator, response_date                         │
│  └─ created_at, updated_at, published_at                         │
│                                                                    │
│  Indexes:                                                         │
│  ├─ tours(status, is_active) - быстрый поиск                     │
│  ├─ tours(operator_id) - туры оператора                          │
│  ├─ reviews(tour_id, status) - отзывы тура                       │
│  ├─ reviews(user_id) - отзывы пользователя                       │
│  └─ tours(activity, difficulty) - фильтрование                   │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных - Создание тура

```
┌─────────────────┐
│  POST /tours    │
│  + TourCreate   │
└────────┬────────┘
         │ (JSON body)
         ▼
┌─────────────────────────────────────────┐
│  TourService.create()                   │
│  ├─ Validate input                      │
│  ├─ Save to DB                          │
│  └─ Publish event: tour.created         │
└────────┬────────────────────────────────┘
         │
         ├─────────────────────────────┬──────────────────────────┐
         │                             │                          │
         ▼                             ▼                          ▼
┌──────────────────┐  ┌───────────────────────┐  ┌──────────────────┐
│ EventBusService  │  │ MonitoringService     │  │ Return Tour      │
│ + publish()      │  │ + log()               │  │ + Cache (1h)     │
│                  │  │ + recordMetric()      │  │                  │
└──────────────────┘  └───────────────────────┘  └──────────────────┘
         │
         ▼ (tour.created event)
┌──────────────────────────────┐
│ Subscribers listening for:   │
│ - tour.created               │
│ - Send notifications         │
│ - Update analytics           │
│ - Trigger workflows          │
└──────────────────────────────┘
```

---

## 🔍 Поток данных - Поиск с фасетами

```
┌──────────────────────────────────────┐
│  GET /search?q=hiking&advanced=true  │
│  + query + filters                   │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│  SearchService.advancedSearch()  │
└────────┬───────────────────────┬─┘
         │                       │
         ▼                       ▼
┌──────────────────────┐  ┌──────────────────┐
│ executeAdvancedSearch│  │  getFacets()     │
│ + Full-text search   │  │ + Count by       │
│ + Apply filters      │  │   activity       │
│ + Sort & paginate    │  │   difficulty     │
│ + Return tours       │  │   price range    │
│                      │  │   rating         │
│                      │  │   operators      │
└────────┬─────────────┘  └────────┬─────────┘
         │                         │
         ├─────────────────┬───────┘
         │                 │
         ▼                 ▼
┌────────────────────────────────────┐
│  Cache result (30 min)             │
└────────┬─────────────────────────┬─┘
         │                         │
         ▼                         ▼
┌──────────────────┐  ┌────────────────────┐
│ tours[]          │  │ facets             │
│ + pagination     │  │ + activities[]     │
│ + total count    │  │ + difficulties[]   │
│ + hasMore        │  │ + priceRanges[]    │
│ + executionTime  │  │ + ratings[]        │
│                  │  │ + operators[]      │
└────────┬─────────┘  └────────┬───────────┘
         │                     │
         └──────────┬──────────┘
                    │
                    ▼
         ┌──────────────────────┐
         │ JSON Response (200)  │
         └──────────────────────┘
```

---

## 🏗️ Иерархия типов

```
Tour (основной интерфейс)
├─ id: string
├─ title: string
├─ description: string
├─ activity: TourActivity ("hiking" | "mountaineering" | ...)
├─ difficulty: TourDifficulty ("easy" | "moderate" | "hard" | "extreme")
├─ duration: number (часы)
├─ minParticipants: number
├─ maxParticipants: number
├─ priceFrom: number (руб)
├─ priceTo?: number
├─ rating: number (0-5)
├─ reviewsCount: number
├─ status: TourStatus ("draft" | "published" | "archived")
├─ operatorId: string
├─ operatorName: string
├─ operatorRating: number
├─ images: string[] (URLs)
├─ equipmentIncluded: string[]
├─ equipmentRequired: string[]
├─ tags: string[]
└─ metadata: { createdAt, updatedAt, publishedAt, viewCount }

TourCreate (DTO для создания)
├─ (все поля кроме: id, rating, status, createdAt, updatedAt, publishedAt, viewCount)
└─ operatorId: string (из headers)

TourUpdate (DTO для обновления)
└─ (все поля опциональные)

TourSearchParams
├─ query?: string
├─ filters?: TourFilters
├─ sortBy?: "rating" | "price" | "duration" | "newest" | "popular"
├─ limit: number (max 100)
└─ offset: number

TourStats
├─ totalBookings: number
├─ totalRevenue: number
├─ averageRating: number
├─ totalReviews: number
├─ conversionRate: number (%)
└─ viewCount: number
```

---

## 🔐 Проверки безопасности

```
                    │
                    ▼
        ┌──────────────────────┐
        │  Request arrives     │
        │  to API endpoint     │
        └──────────┬───────────┘
                   │
         ┌─────────▼─────────┐
         │ Has auth header?  │
         │ (x-user-id OR    │
         │  x-operator-id)  │
         └─────────┬─────────┘
                   │
        ┌──────────▼──────────┐
        │  Check user role    │
        │  (x-user-role)      │
        └─────────┬───────────┘
                   │
        ┌──────────▼──────────┐
        │  Has permission     │
        │  for operation?     │
        │  (user/operator/    │
        │   moderator/admin)  │
        └─────────┬───────────┘
                   │
        ┌──────────▼──────────┐
        │  Validate input     │
        │  data (DTO)         │
        └─────────┬───────────┘
                   │
        ┌──────────▼──────────┐
        │  Check ownership    │
        │  (if needed)        │
        └─────────┬───────────┘
                   │
        ┌──────────▼──────────┐
        │  Execute operation  │
        │  in database        │
        └─────────┬───────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │  Return 200/201      │
        │  or error code       │
        │  (400/401/403/404)   │
        └──────────────────────┘
```

---

## 📊 Кеширование

```
┌─────────────────────────────────┐
│  Operation requested            │
└────────────┬────────────────────┘
             │
      ┌──────▼───────┐
      │ Check cache? │
      └──────┬───────┘
             │
    ┌────────▼────────┐
    │   Cache hit?    │ ─────────────┐
    └────────┬────────┘              │
             │ No                    │ Yes
             │                       │
    ┌────────▼─────────────┐        │
    │ Execute operation    │        │
    │ (database query)     │        │
    │ (expensive compute)  │        │
    └────────┬─────────────┘        │
             │                      │
    ┌────────▼──────────────┐       │
    │ Store in cache        │       │
    │ TTL:                  │       │
    │ - Tours: 1 hour       │       │
    │ - Reviews: 30 min     │       │
    │ - Recommendations: 1h │       │
    └────────┬──────────────┘       │
             │                      │
             └──────────┬───────────┘
                        │
                 ┌──────▼─────────┐
                 │ Return result  │
                 │ from cache     │
                 └────────────────┘
```

---

## 🎯 Модерация отзывов

```
User submits review
        │
        ▼
┌──────────────────────────┐
│ ReviewService.create()   │
│ Status: "pending"        │
└────────┬─────────────────┘
         │
         ├─────────────────────────┐
         │                         │
         ▼                         ▼
    ┌────────────┐         ┌──────────────┐
    │ Publish:   │         │ Notify admin │
    │ review.    │         │ of pending   │
    │ created    │         │ review       │
    └────────────┘         └──────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Admin/Moderator reviews submission│
│                                  │
│ POST /reviews/[id]/approve    ──┐│
│ POST /reviews/[id]/reject     ──┤│
│                                ├┼──────┐
│                                │└────┐ │
└────────────────────────────────┘     │ │
                                       ▼ ▼
                        ┌──────────────────────┐
                        │ Update status        │
                        │ Notify user          │
                        │ Update tour rating   │
                        │ Publish event        │
                        └──────────────────────┘
                                       │
                                       ▼
                        ┌──────────────────────┐
                        │ Review published OR  │
                        │ Review rejected      │
                        └──────────────────────┘
```

---

## 🚀 Масштабируемость

```
Single Instance (dev/staging)
├─ All services in one process
├─ In-memory cache
├─ Single database connection
└─ Works fine for testing

                    │
                    │ Production scaling
                    ▼

Multi-Instance (production)
├─ Services replicated
├─ Shared Redis cache
│  └─ All instances share cache
├─ Database connection pool
│  └─ Efficient connection reuse
├─ Load balancer
│  └─ Distribute requests
└─ Event streaming
   └─ Services communicate via events
```

---

**Диаграмма создана:** 28 января 2026  
**Для:** KamHub Discovery Pillar  
**Версия:** 1.0.0
