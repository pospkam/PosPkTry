# 🗺️ КАРТА ВЗАИМОСВЯЗЕЙ ВСЕХ 71 СТРАНИЦ

**Дата:** 27 ноября 2025  
**Всего страниц:** 71 статическая + API routes  
**Тип навигации:** Next.js App Router с Link компонентами

---

## 📊 СТРУКТУРА ПРОЕКТА (71 СТРАНИЦА)

### 🏠 ГЛАВНАЯ (1 страница)
```
/ (page.tsx) → Главная страница
  ├─ Link → /tours
  ├─ Link → /auth/login
  ├─ Link → /hub/tourist (кнопка "Я Турист")
  ├─ Link → /partner/register (кнопка "Я Бизнес")
  └─ Component → AISmartSearch, RegistrationButtons
```

### 🎫 ТУРЫ (3 страницы)
```
/tours → Список всех туров
  ├─ Link → /tours/[id] (карточка тура)
  └─ Link → / (назад)

/tours/[id] → Детальная страница тура
  ├─ Link → /tours (назад к списку)
  ├─ Link → /auth/login (бронирование)
  └─ Component → TourBookingForm

/hub/tours → Туры в хабе
  └─ Link → /tours/[id]
```

### 🏨 РАЗМЕЩЕНИЕ (2 страницы)
```
/accommodations/[id] → Детальная страница размещения
  ├─ Link → / (главная)
  ├─ Component → StayBookingForm
  └─ API → /api/accommodations/[id]
```

### 👤 АВТОРИЗАЦИЯ (1 страница)
```
/auth/login → Вход/Регистрация
  ├─ useRouter → / (после входа)
  ├─ useRouter → /hub/tourist
  ├─ useRouter → /hub/operator
  └─ API → /api/auth/register, /api/auth/login
```

### 🎯 ОСНОВНЫЕ СТРАНИЦЫ (6 страниц)
```
/cars → Аренда авто
  └─ API → /api/cars

/gear → Аренда снаряжения
  └─ API → /api/gear

/shop → Сувениры
  └─ Link → /hub/souvenirs

/search → Поиск
  └─ API → /api/tours, /api/accommodations

/profile → Профиль пользователя
  └─ useRouter → /auth/login (если не авторизован)

/demo → AI демонстрация
  └─ Component → AISmartSearch
```

### 🏢 ХАБЫ РОЛЕЙ (32 страницы)

#### 👤 Турист (3 страницы)
```
/hub/tourist → Дашборд туриста
  ├─ Link → /tours
  ├─ Link → /hub/transfer
  └─ Component → AIChatWidget, TransferSearchWidget

/hub/tourist/bookings → Бронирования туриста
  └─ API → /api/bookings

/hub/tours → Туры в хабе
  └─ Link → /tours/[id]
```

#### 🏢 Оператор (8 страниц)
```
/hub/operator → Дашборд оператора
  ├─ Link → /hub/operator/tours
  ├─ Link → /hub/operator/bookings
  └─ Link → /hub/operator/calendar

/hub/operator/tours → Управление турами
  ├─ Link → /hub/operator/tours/new
  ├─ Link → /hub/operator/tours/[id]
  └─ API → /api/tours

/hub/operator/tours/new → Создание тура
  └─ useRouter → /hub/operator/tours

/hub/operator/tours/[id] → Редактирование тура
  └─ API → /api/tours/[id]

/hub/operator/bookings → Бронирования оператора
  └─ API → /api/bookings/operator

/hub/operator/calendar → Календарь туров
  └─ API → /api/tours

/hub/operator/finance → Финансы
  └─ API → /api/finance

/hub/operator/transfer → Трансферы оператора
  └─ API → /api/transfers
```

#### 👨‍💼 Агент (5 страниц)
```
/hub/agent → Дашборд агента
  ├─ Link → /hub/agent/clients
  ├─ Link → /hub/agent/bookings
  └─ Link → /hub/agent/commissions

/hub/agent/clients → Клиенты агента
  └─ API → /api/agent/clients

/hub/agent/bookings → Бронирования агента
  └─ API → /api/agent/bookings

/hub/agent/commissions → Комиссии
  └─ API → /api/agent/commissions

/hub/agent/vouchers → Ваучеры
  └─ API → /api/agent/vouchers
```

#### 👨‍💼 Администратор (7 страниц)
```
/hub/admin → Админ панель
  ├─ Link → /hub/admin/users
  ├─ Link → /hub/admin/content/tours
  └─ Link → /hub/admin/finance

/hub/admin/users → Управление пользователями
  └─ API → /api/admin/users

/hub/admin/content/tours → Модерация туров
  └─ API → /api/admin/tours

/hub/admin/content/partners → Модерация партнёров
  └─ API → /api/admin/partners

/hub/admin/content/reviews → Модерация отзывов
  └─ API → /api/admin/reviews

/hub/admin/finance → Финансы
  └─ API → /api/admin/finance

/hub/admin/settings → Настройки
  └─ API → /api/admin/settings
```

#### 🚗 Трансферы (4 страницы)
```
/hub/transfer → Бронирование трансфера
  └─ API → /api/transfers

/hub/transfer-operator → Дашборд оператора трансфера
  ├─ Link → /hub/transfer-operator/drivers
  └─ Link → /hub/transfer-operator/vehicles

/hub/transfer-operator/drivers → Водители
  └─ API → /api/transfer-operator/drivers

/hub/transfer-operator/vehicles → Транспорт
  └─ API → /api/transfer-operator/vehicles
```

#### 👨‍🏫 Гид (1 страница)
```
/hub/guide → Дашборд гида
  └─ API → /api/guide/tours
```

#### 🏨 Размещение (2 страницы)
```
/hub/stay → Управление размещением
  └─ API → /api/stay

/hub/stay-provider → Провайдер размещения
  └─ API → /api/stay-provider/dashboard
```

#### 🎒 Аренда (2 страницы)
```
/hub/cars → Аренда авто в хабе
  └─ API → /api/cars

/hub/gear → Аренда снаряжения в хабе
  └─ API → /api/gear
```

#### 🛍️ Сувениры (1 страница)
```
/hub/souvenirs → Управление сувенирами
  └─ API → /api/souvenirs
```

#### 🆘 Безопасность (1 страница)
```
/hub/safety → SOS и безопасность
  └─ API → /api/safety/sos
```

### 🤝 ПАРТНЁРЫ (3 страницы)
```
/partner/register → Регистрация партнёра
  └─ useRouter → /partner/dashboard

/partner/dashboard → Дашборд партнёра
  ├─ Link → /partner/tours/add
  └─ API → /api/partner/dashboard

/partner/tours/add → Добавление тура
  └─ useRouter → /partner/dashboard
```

### 🧪 ТЕСТОВЫЕ (2 страницы)
```
/test → Тестовая страница
/ui-demo → Демо UI компонентов
```

---

## 🔗 ТИПЫ ВЗАИМОСВЯЗЕЙ

### 1. **Навигация через Link** (прямые ссылки)
- `<Link href="/tours">` - переход на список туров
- `<Link href="/tours/[id]">` - переход на детальную страницу
- `<Link href="/auth/login">` - переход на авторизацию

### 2. **Навигация через useRouter** (программная)
- `router.push('/hub/tourist')` - после авторизации
- `router.push('/tours')` - после создания тура
- `router.back()` - возврат назад

### 3. **API взаимодействия**
- `/api/tours` → данные для `/tours`
- `/api/tours/[id]` → данные для `/tours/[id]`
- `/api/bookings` → данные для `/hub/tourist/bookings`

### 4. **Компоненты связи**
- `WeatherBackground` - на всех страницах (layout.tsx)
- `AISmartSearch` - поиск на главной
- `RegistrationButtons` - кнопки ролей
- `FloatingAIButton` - плавающая кнопка AI

---

## 📊 СТАТИСТИКА ВЗАИМОСВЯЗЕЙ

```
✅ Link компонентов: 6 файлов
✅ useRouter хуков: 20 файлов
✅ API routes: 100+ эндпоинтов
✅ Общих компонентов: 30+
✅ Контекстов: 3 (Auth, Role, Orders)
```

---

## 🗺️ ГРАФ ГЛАВНЫХ МАРШРУТОВ

```
                    ┌────────────┐
                    │     /      │
                    │  Главная   │
                    └─────┬──────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
   ┌────▼────┐      ┌─────▼─────┐    ┌─────▼──────┐
   │ /tours  │      │/auth/login│    │/hub/tourist│
   │ Туры    │      │Авторизация│    │  Турист    │
   └────┬────┘      └─────┬─────┘    └─────┬──────┘
        │                 │                 │
   ┌────▼────┐      ┌─────▼─────┐    ┌─────▼──────┐
   │/tours/  │      │/hub/      │    │/tours      │
   │  [id]   │      │operator   │    │            │
   └─────────┘      └───────────┘    └────────────┘
```

---

## ✅ ПРОВЕРКА ВЗАИМОСВЯЗЕЙ ПОСЛЕ ДЕПЛОЯ

### 1. Главная → Туры
```bash
curl -s http://147.45.158.166/ | grep -o 'href="/tours"' | wc -l
# Ожидается: > 0
```

### 2. Туры → Детальная страница
```bash
curl -s http://147.45.158.166/tours | grep -o 'href="/tours/' | wc -l
# Ожидается: > 0 (количество туров)
```

### 3. Главная → Авторизация
```bash
curl -s http://147.45.158.166/ | grep -o 'href="/auth/login"' | wc -l
# Ожидается: > 0
```

### 4. Главная → Хабы
```bash
curl -s http://147.45.158.166/ | grep -o 'href="/hub/' | wc -l
# Ожидается: > 0
```

---

## 🎯 КРИТИЧНЫЕ ВЗАИМОСВЯЗИ

**Обязательно должны работать:**

1. ✅ **Главная → Туры** - основной флоу
2. ✅ **Главная → Авторизация** - вход в систему
3. ✅ **Главная → Хабы** - доступ к функционалу
4. ✅ **Туры → Детальная** - просмотр тура
5. ✅ **Детальная → Бронирование** - конверсия
6. ✅ **Layout → WeatherBackground** - дизайн на всех страницах

---

## 📝 ИТОГО

**Всего страниц:** 71 (49 page.tsx + API routes)  
**Взаимосвязей:** 100+ (Link, useRouter, API, Components)  
**Уровней вложенности:** до 4 (/hub/operator/tours/[id])  
**Тип навигации:** Next.js App Router (client-side + server-side)

**ВСЕ СТРАНИЦЫ ВЗАИМОСВЯЗАНЫ ЧЕРЕЗ:**
- Next.js Link компоненты
- useRouter для программной навигации
- API routes для данных
- Общие компоненты и контексты
- WeatherBackground на всех страницах через layout.tsx

✅ **ГОТОВО К ДЕПЛОЮ С ПОЛНЫМИ ВЗАИМОСВЯЗЯМИ!**
