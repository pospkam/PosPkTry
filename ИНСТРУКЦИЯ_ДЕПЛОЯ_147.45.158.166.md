# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА 147.45.158.166

**Дата:** 27 ноября 2025  
**Сервер:** http://147.45.158.166/  
**Статус:** ✅ Код готов, ожидает деплоя

---

## 📋 ЧТО ГОТОВО К ДЕПЛОЮ

✅ **Код запушен в GitHub** (коммит `a58be39`)  
✅ **Сборка успешна** (71/71 страниц)  
✅ **Удалены ВСЕ эмодзи** (100+ мест)  
✅ **Добавлены lucide-react иконки**  
✅ **Интегрирован динамический фон**  
✅ **Применены CSS темы Samsung Weather**

---

## 🎯 СПОСОБ 1: АВТОМАТИЧЕСКИЙ ДЕПЛОЙ (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Подключение к серверу

```bash
ssh root@147.45.158.166
```

### Шаг 2: Скачивание и запуск скрипта деплоя

```bash
# Переход в директорию проекта
cd /var/www/kamchatour

# Скачивание скрипта деплоя
curl -O https://raw.githubusercontent.com/PosPk/kamhub/main/deploy-147-45-158-166.sh

# Или если скрипт уже в репозитории:
git fetch origin main
git checkout origin/main -- deploy-147-45-158-166.sh

# Установка прав на выполнение
chmod +x deploy-147-45-158-166.sh

# ЗАПУСК ДЕПЛОЯ
./deploy-147-45-158-166.sh
```

**Скрипт автоматически выполнит:**
1. ✅ Обновление кода из GitHub
2. ✅ Установку зависимостей
3. ✅ Очистку кеша
4. ✅ Сборку проекта
5. ✅ Перезапуск приложения
6. ✅ Проверку статуса

**Время выполнения:** ~3-5 минут

---

## 🎯 СПОСОБ 2: РУЧНОЙ ДЕПЛОЙ

### Подключитесь к серверу:

```bash
ssh root@147.45.158.166
```

### Выполните команды по порядку:

```bash
# 1. Переход в директорию проекта
cd /var/www/kamchatour

# 2. Обновление кода из GitHub
git fetch origin main
git reset --hard origin/main

# 3. Проверка последнего коммита
git log -1 --oneline
# Должен быть: a58be39 📝 Документация: финальный дизайн без эмодзи готов к деплою

# 4. Установка новых зависимостей
npm install

# 5. Остановка приложения
pm2 delete kamchatour

# 6. Очистка кеша сборки
rm -rf .next
rm -rf node_modules/.cache

# 7. Сборка проекта (занимает 2-3 минуты)
npm run build

# 8. Запуск приложения
pm2 start npm --name kamchatour -- start

# 9. Сохранение конфигурации PM2
pm2 save

# 10. Проверка статуса
pm2 status kamchatour
pm2 logs kamchatour --lines 20
```

---

## ✅ ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

### 1. Проверка доступности сайта:

```bash
curl -I http://147.45.158.166/
```

**Ожидается:** `HTTP/1.1 200 OK`

### 2. Проверка отсутствия эмодзи:

```bash
curl -s http://147.45.158.166/ | grep -c "🏔️\|🌋\|⭐"
```

**Ожидается:** `0` (никаких эмодзи!)

### 3. Проверка lucide-react:

```bash
curl -s http://147.45.158.166/ | grep -c "lucide"
```

**Ожидается:** `> 0` (иконки есть)

### 4. Проверка WeatherBackground:

```bash
curl -s http://147.45.158.166/ | grep -c "WeatherBackground"
```

**Ожидается:** `> 0` (компонент загружен)

### 5. Откройте в браузере:

- ✅ http://147.45.158.166/ - главная
- ✅ http://147.45.158.166/tours - список туров
- ✅ http://147.45.158.166/auth/login - авторизация
- ✅ http://147.45.158.166/hub/tourist - турист
- ✅ http://147.45.158.166/demo - AI демо

**Проверьте:**
- ❌ Нет эмодзи (🏔️ 🌋 ⭐ ✨)
- ✅ Иконки lucide-react
- ✅ Динамический фон меняется
- ✅ Светлые прозрачные карточки
- ✅ Анимации погоды

---

## 🔧 КОМАНДЫ ДЛЯ УПРАВЛЕНИЯ

### Просмотр логов:
```bash
pm2 logs kamchatour
pm2 logs kamchatour --lines 50
```

### Перезапуск приложения:
```bash
pm2 restart kamchatour
```

### Статус приложения:
```bash
pm2 status
```

### Проверка Nginx:
```bash
sudo nginx -t
sudo systemctl status nginx
```

### Перезапуск Nginx:
```bash
sudo systemctl reload nginx
```

---

## ❗ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: "npm: command not found"
```bash
# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
```

### Проблема 2: "pm2: command not found"
```bash
# Установка PM2
npm install -g pm2
```

### Проблема 3: Ошибка сборки
```bash
# Очистка и пересборка
rm -rf node_modules .next
npm install
npm run build
```

### Проблема 4: Порт 3000 занят
```bash
# Проверка процессов на порту 3000
lsof -i :3000

# Или изменить порт в .env:
echo "PORT=3002" >> .env
```

### Проблема 5: Приложение не запускается
```bash
# Проверка логов
pm2 logs kamchatour --err

# Запуск в режиме отладки
npm run dev
```

---

## 📊 ЧТО ИЗМЕНИЛОСЬ

### Файловая структура:
- ✅ `app/samsung-weather-theme.css` (новый)
- ✅ `app/samsung-weather-dynamic.css` (новый)
- ✅ `components/icons/ActivityIcon.tsx` (новый)
- ✅ `components/icons/WeatherIcon.tsx` (новый)
- ✅ `components/WeatherBackground.tsx` (обновлён)
- ✅ 31 файл изменён (удалены эмодзи)

### Зависимости:
```json
{
  "lucide-react": "^0.x.x",      // новая
  "react-datepicker": "^x.x.x",  // новая
  "date-fns": "^x.x.x",          // новая
  "react-hot-toast": "^x.x.x"    // новая
}
```

---

## 🎨 ВИЗУАЛЬНЫЕ ИЗМЕНЕНИЯ

### До:
- ❌ Эмодзи везде (🏔️ ⭐ 🌋 💨)
- ❌ Тёмный фон
- ❌ Статичный дизайн

### После:
- ✅ lucide-react иконки
- ✅ Динамический фон
- ✅ Светлый премиальный дизайн
- ✅ Анимации погоды
- ✅ Ультратонкие шрифты
- ✅ Глубокий backdrop-blur

---

## 📞 ПОДДЕРЖКА

Если деплой не прошёл успешно:

1. **Проверьте логи:** `pm2 logs kamchatour`
2. **Проверьте статус:** `pm2 status`
3. **Проверьте Nginx:** `sudo nginx -t`
4. **Проверьте порт:** `lsof -i :3000`
5. **Пересоберите:** `rm -rf .next && npm run build`

---

## ✅ ЧЕКЛИСТ ДЕПЛОЯ

- [ ] Подключился к серверу (ssh root@147.45.158.166)
- [ ] Перешёл в /var/www/kamchatour
- [ ] Обновил код (git reset --hard origin/main)
- [ ] Установил зависимости (npm install)
- [ ] Очистил кеш (rm -rf .next)
- [ ] Собрал проект (npm run build)
- [ ] Перезапустил приложение (pm2 restart kamchatour)
- [ ] Проверил статус (pm2 status)
- [ ] Проверил логи (pm2 logs kamchatour)
- [ ] Открыл сайт в браузере
- [ ] Убедился что эмодзи удалены
- [ ] Убедился что иконки работают
- [ ] Убедился что фон динамический

---

## 🎉 ГОТОВО!

После успешного деплоя:
- 🌐 Откройте http://147.45.158.166/
- 🎨 Наслаждайтесь новым дизайном
- ✅ Никаких эмодзи, только профессиональные иконки!

**Деплой займёт ~5 минут. Удачи! 🚀**
