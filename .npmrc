# Skip optional native binaries (onnxruntime, sharp platform variants)
# They're excluded from standalone build anyway (outputFileTracingExcludes)
omit=optional
ignore-scripts=true
fund=false
audit=false
loglevel=warn
