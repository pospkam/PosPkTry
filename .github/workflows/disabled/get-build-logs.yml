name: Get Build Logs

on:
  workflow_dispatch:

jobs:
  get-logs:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📦 Get Last Deployment Info
        run: |
          echo "=========================================="
          echo "📦 ПОСЛЕДНИЙ DEPLOYMENT"
          echo "=========================================="
          
          curl -s "https://api.timeweb.cloud/api/v1/apps/125051/deployments?limit=1" \
            -H "Authorization: Bearer ${{ secrets.TIMEWEB_TOKEN1 }}" \
            -H "Content-Type: application/json"
          
          echo ""
          echo "=========================================="
          echo "🔨 BUILD LOGS"
          echo "=========================================="
          
          curl -s "https://api.timeweb.cloud/api/v1/apps/125051/logs?type=build&limit=100" \
            -H "Authorization: Bearer ${{ secrets.TIMEWEB_TOKEN1 }}" \
            -H "Content-Type: application/json"
          
          echo ""
          echo "=========================================="
          echo "🚀 RUNTIME LOGS"
          echo "=========================================="
          
          curl -s "https://api.timeweb.cloud/api/v1/apps/125051/logs?type=runtime&limit=100" \
            -H "Authorization: Bearer ${{ secrets.TIMEWEB_TOKEN1 }}" \
            -H "Content-Type: application/json"
          
          echo ""
          echo "=========================================="
          echo "⚙️ APP CONFIG"
          echo "=========================================="
          
          curl -s "https://api.timeweb.cloud/api/v1/apps/125051" \
            -H "Authorization: Bearer ${{ secrets.TIMEWEB_TOKEN1 }}" \
            -H "Content-Type: application/json"
