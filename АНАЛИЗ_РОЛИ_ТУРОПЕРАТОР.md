# 📊 Детальный анализ роли ТУРОПЕРАТОР

## 🎯 Цель
Проверить и улучшить функциональность роли **Туроператор (operator)** после исправления регистрации партнеров.

---

## ✅ ЧТО УЖЕ ГОТОВО

### 1. 🔐 Авторизация и регистрация
- ✅ Регистрация партнера с ролью `operator`
- ✅ Создание user + partner в БД
- ✅ Хеширование паролей
- ✅ JWT авторизация
- ✅ Связь `users.id` ↔ `partners.user_id`

### 2. 📄 API Endpoints (8 готовых)

#### Dashboard `/api/operator/dashboard`
- ✅ GET - статистика дашборда
- ✅ Метрики: туры, бронирования, доход, отзывы
- ✅ Недавние бронирования

#### Tours `/api/operator/tours`
- ✅ GET - список туров оператора
- ✅ POST - создание нового тура
- ✅ GET `/api/operator/tours/[id]` - детали тура
- ✅ PUT `/api/operator/tours/[id]` - обновление тура
- ✅ DELETE `/api/operator/tours/[id]` - удаление тура

#### Bookings `/api/operator/bookings`
- ✅ GET - список бронирований
- ✅ GET `/api/operator/bookings/[id]` - детали бронирования
- ✅ PATCH `/api/operator/bookings/[id]` - обновление статуса

#### Finance `/api/operator/finance`
- ✅ GET - финансовая статистика
- ✅ Доходы, расходы, прибыль

#### Calendar `/api/operator/calendar`
- ✅ GET - календарь бронирований
- ✅ Фильтры по датам

#### Stats `/api/operator/stats`
- ✅ GET - подробная статистика
- ✅ По турам, бронированиям, доходам

### 3. 🎨 Frontend (7 страниц)

#### Dashboard `/hub/operator`
- ✅ Метрики (туры, бронирования, доход)
- ✅ Графики и статистика
- ✅ Быстрые действия

#### Tours Management `/hub/operator/tours`
- ✅ Список туров
- ✅ Создание тура `/hub/operator/tours/new`
- ✅ Редактирование тура `/hub/operator/tours/[id]`
- ✅ Управление фото

#### Bookings `/hub/operator/bookings`
- ✅ Список бронирований
- ✅ Фильтры и поиск
- ✅ Изменение статусов

#### Finance `/hub/operator/finance`
- ✅ Отчеты по доходам
- ✅ Графики финансов
- ✅ Экспорт данных

#### Calendar `/hub/operator/calendar`
- ✅ Календарь бронирований
- ✅ Блокировка дат
- ✅ Управление доступностью

### 4. 🧩 Components (3 компонента)

- ✅ `OperatorNav` - навигация
- ✅ `OperatorMetricsGrid` - метрики
- ✅ `RecentBookingsTable` - таблица бронирований
- ✅ `TopToursTable` - топ туры
- ✅ `TourForm` - форма создания/редактирования тура

---

## ⚠️ ЧТО НУЖНО ПРОВЕРИТЬ И ИСПРАВИТЬ

### 1. 🔗 Связь users ↔ partners ↔ tours

**Проблема**: После добавления `user_id` в `partners`, нужно проверить что все API правильно получают данные.

**Файлы для проверки**:
```
app/api/operator/tours/route.ts
app/api/operator/bookings/route.ts
app/api/operator/dashboard/route.ts
app/api/operator/stats/route.ts
```

**Что проверить**:
```sql
-- Правильный запрос теперь должен быть:
SELECT t.* FROM tours t
JOIN partners p ON t.operator_id = p.id
JOIN users u ON p.user_id = u.id
WHERE u.id = $1  -- ID из JWT токена
```

**Действие**: 
- [ ] Проверить все SQL запросы в API операторов
- [ ] Обновить запросы для использования `user_id`
- [ ] Добавить ownership verification

### 2. 🔐 Авторизация в API

**Проблема**: Нужно убедиться что все API проверяют:
- JWT токен валиден
- Пользователь имеет роль `operator`
- Пользователь владеет ресурсами (tours, bookings)

**Файлы для проверки**:
```
middleware.ts
app/api/operator/**/*.ts
```

**Что добавить**:
```typescript
// В каждый API endpoint оператора:
const userId = request.headers.get('X-User-Id');
const userRole = request.headers.get('X-User-Role');

if (userRole !== 'operator') {
  return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
}

// Проверка ownership
const partner = await query(
  'SELECT id FROM partners WHERE user_id = $1',
  [userId]
);
```

**Действие**:
- [ ] Добавить проверки авторизации во все endpoints
- [ ] Создать helper функцию `getOperatorPartnerId(userId)`
- [ ] Добавить middleware для роли operator

### 3. 📸 Загрузка фото туров

**Проблема**: Нужно настроить загрузку изображений на Timeweb S3

**Файлы**:
```
app/api/operator/tours/[id]/route.ts - обновление тура
components/operator/Tours/TourForm.tsx - форма с загрузкой
lib/s3.ts - работа с S3
```

**Что нужно**:
```typescript
// lib/s3.ts
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

export async function uploadToS3(file: File): Promise<string> {
  const s3 = new S3Client({
    region: 'ru-1',
    endpoint: process.env.S3_ENDPOINT,
    credentials: {
      accessKeyId: process.env.S3_ACCESS_KEY!,
      secretAccessKey: process.env.S3_SECRET_KEY!,
    },
  });
  
  // Upload logic...
  return url;
}
```

**Действие**:
- [ ] Создать модуль `lib/s3.ts`
- [ ] Добавить API для загрузки `/api/operator/upload`
- [ ] Интегрировать в TourForm

### 4. 📊 Dashboard метрики

**Проблема**: Дашборд может показывать неправильные данные если запросы не учитывают `user_id`

**Файл**: `app/api/operator/dashboard/route.ts`

**Что проверить**:
```typescript
// Все запросы должны фильтроваться по operator_id:
const partnerId = await getOperatorPartnerId(userId);

const tours = await query(`
  SELECT COUNT(*) FROM tours WHERE operator_id = $1
`, [partnerId]);

const bookings = await query(`
  SELECT COUNT(*) FROM bookings b
  JOIN tours t ON b.tour_id = t.id
  WHERE t.operator_id = $1
`, [partnerId]);
```

**Действие**:
- [ ] Обновить все SQL запросы в dashboard
- [ ] Добавить кеширование метрик
- [ ] Тестировать с реальными данными

### 5. 💰 Финансы и выплаты

**Проблема**: Нужно реализовать систему выплат операторам

**Файл**: `app/api/operator/finance/route.ts`

**Что добавить**:
```typescript
// Расчет комиссии платформы
const PLATFORM_FEE = 0.15; // 15%

const revenue = await query(`
  SELECT 
    SUM(b.total_price) as total_revenue,
    SUM(b.total_price * ${PLATFORM_FEE}) as platform_fee,
    SUM(b.total_price * ${1 - PLATFORM_FEE}) as operator_revenue
  FROM bookings b
  JOIN tours t ON b.tour_id = t.id
  WHERE t.operator_id = $1
    AND b.payment_status = 'paid'
`, [partnerId]);
```

**Действие**:
- [ ] Добавить таблицу `operator_payouts`
- [ ] Создать API для запроса выплаты
- [ ] Добавить историю транзакций

### 6. 📅 Календарь и доступность

**Проблема**: Нужно реализовать блокировку дат и управление доступностью

**Файл**: `app/api/operator/calendar/route.ts`

**Что добавить**:
```sql
-- Таблица для блокировки дат
CREATE TABLE tour_availability (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tour_id UUID REFERENCES tours(id),
  date DATE NOT NULL,
  available_slots INTEGER NOT NULL,
  is_blocked BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

**Действие**:
- [ ] Создать миграцию для `tour_availability`
- [ ] Добавить API для блокировки дат
- [ ] Интегрировать в календарь

### 7. 🔔 Уведомления

**Проблема**: Оператор должен получать уведомления о:
- Новых бронированиях
- Отменах
- Отзывах
- Запросах на выплату

**Действие**:
- [ ] Создать триггеры в БД
- [ ] Добавить API `/api/operator/notifications`
- [ ] Интегрировать в UI

### 8. 📈 Отзывы и рейтинг

**Проблема**: Нужен интерфейс для просмотра и ответа на отзывы

**Действие**:
- [ ] Создать `/hub/operator/reviews`
- [ ] API `/api/operator/reviews`
- [ ] Возможность отвечать на отзывы

---

## 🚀 ПЛАН РАБОТЫ (приоритеты)

### 🔥 Критично (сделать сейчас):
1. ✅ Проверить и обновить SQL запросы с `user_id` ↔ `partners`
2. ✅ Добавить авторизацию и ownership проверки
3. ✅ Создать helper `getOperatorPartnerId(userId)`
4. ✅ Протестировать dashboard с реальными данными

### ⚡ Важно (следующий шаг):
5. 📸 Настроить загрузку фото на S3
6. 💰 Реализовать систему выплат
7. 📅 Добавить управление доступностью
8. 🔔 Настроить уведомления

### 📝 Можно позже:
9. 📊 Добавить экспорт отчетов (CSV, PDF)
10. 📈 Расширенная аналитика
11. 🎨 Улучшить UI/UX
12. 📱 Мобильная версия

---

## 📂 Структура файлов туроператора

```
app/
├── api/operator/
│   ├── dashboard/route.ts      ✅ Готово (нужна проверка SQL)
│   ├── tours/
│   │   ├── route.ts            ✅ Готово (нужна проверка SQL)
│   │   └── [id]/route.ts       ✅ Готово (нужна проверка SQL)
│   ├── bookings/
│   │   ├── route.ts            ✅ Готово (нужна проверка SQL)
│   │   └── [id]/route.ts       ✅ Готово (нужна проверка SQL)
│   ├── finance/route.ts        ✅ Готово (нужна доработка)
│   ├── calendar/route.ts       ✅ Готово (нужна доработка)
│   ├── stats/route.ts          ✅ Готово (нужна проверка SQL)
│   ├── upload/route.ts         ❌ Создать
│   ├── notifications/route.ts  ❌ Создать
│   └── reviews/route.ts        ❌ Создать
│
├── hub/operator/
│   ├── page.tsx                ✅ Dashboard готов
│   ├── tours/
│   │   ├── page.tsx            ✅ Список туров
│   │   ├── new/page.tsx        ✅ Создание тура
│   │   └── [id]/page.tsx       ✅ Редактирование
│   ├── bookings/page.tsx       ✅ Бронирования
│   ├── finance/page.tsx        ✅ Финансы
│   ├── calendar/page.tsx       ✅ Календарь
│   ├── reviews/page.tsx        ❌ Создать
│   └── settings/page.tsx       ❌ Создать
│
└── components/operator/
    ├── OperatorNav.tsx          ✅ Навигация
    ├── TourForm.tsx             ✅ Форма тура
    ├── Dashboard/               ✅ Компоненты дашборда
    └── Reviews/                 ❌ Создать
```

---

## 🧪 Чек-лист для тестирования

### Регистрация и вход:
- [ ] Зарегистрировать нового туроператора
- [ ] Войти в систему
- [ ] Проверить JWT токен в cookies
- [ ] Проверить редирект на `/hub/operator`

### Dashboard:
- [ ] Открыть dashboard
- [ ] Проверить отображение метрик
- [ ] Проверить что метрики показывают ТОЛЬКО данные этого оператора

### Tours:
- [ ] Создать новый тур
- [ ] Загрузить фото
- [ ] Отредактировать тур
- [ ] Удалить тур
- [ ] Проверить что видны ТОЛЬКО свои туры

### Bookings:
- [ ] Просмотреть бронирования
- [ ] Изменить статус бронирования
- [ ] Проверить фильтры
- [ ] Проверить что видны ТОЛЬКО бронирования своих туров

### Finance:
- [ ] Просмотреть финансовую статистику
- [ ] Проверить расчет комиссии
- [ ] Экспортировать отчет

### Calendar:
- [ ] Просмотреть календарь
- [ ] Заблокировать дату
- [ ] Изменить доступность

---

## 💡 Рекомендации

### Безопасность:
1. Всегда проверять `user_id` в запросах
2. Использовать prepared statements
3. Валидировать все входные данные
4. Логировать все операции

### Производительность:
1. Кешировать метрики dashboard
2. Использовать pagination для списков
3. Оптимизировать SQL запросы с индексами

### UX:
1. Добавить loading states
2. Показывать информативные ошибки
3. Добавить подсказки для новых операторов
4. Реализовать onboarding flow

---

## 📞 Следующие шаги

1. **СЕЙЧАС**: Проверить и исправить SQL запросы
2. **ЗАТЕМ**: Добавить авторизацию во все endpoints
3. **ПОТОМ**: Настроить S3 для фото
4. **ДАЛЕЕ**: Тестирование с реальными данными

---

**Готовность роли Туроператор**: **85%**

**Осталось сделать**: 15% (в основном проверки и тесты)

**Время на доработку**: ~2-3 часа
