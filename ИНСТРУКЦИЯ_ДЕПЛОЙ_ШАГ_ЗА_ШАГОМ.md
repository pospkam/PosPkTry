# 📋 ИНСТРУКЦИЯ ДЕПЛОЙ ШАГ ЗА ШАГОМ
## Вы делаете, я помогаю

---

## ШАГ 1: Создать Git репозиторий (5 минут)

### На вашем компьютере выполните:

```powershell
# 1. Перейдите в папку проекта
cd C:\Users\Администратор.DESKTOP-GDHBNEF\kamhub

# 2. Инициализируйте Git (если еще не сделано)
git init

# 3. Добавьте все файлы
git add .

# 4. Сделайте коммит
git commit -m "Production ready: AI assistants + deploy scripts"

# 5. Создайте репозиторий на GitHub:
# Перейдите на github.com
# Нажмите "New repository"
# Название: kamhub
# Скопируйте команды для push

# 6. Отправьте код в GitHub
git remote add origin https://github.com/ВАШ_USERNAME/kamhub.git
git branch -M main
git push -u origin main
```

**После этого скажите мне ссылку на репозиторий, и я продолжу деплой!**

---

## ШАГ 2: Я сделаю деплой на сервер

После того как вы создадите репозиторий, я:
1. Подключусь к серверу
2. Клонирую проект
3. Применю SQL схемы
4. Запущу приложение
5. Проверю работоспособность

---

## АЛЬТЕРНАТИВА: Ручной деплой (если не хотите Git)

### На вашем компьютере:

```powershell
# 1. Создайте архив проекта
tar -czf kamhub.tar.gz --exclude=node_modules --exclude=.next --exclude=.git .

# 2. Загрузите на сервер через SCP
scp kamhub.tar.gz root@5.129.248.224:/var/www/
```

### На сервере:

```bash
ssh root@5.129.248.224
# Пароль: xQvB1pv?yZTjaR

cd /var/www
tar -xzf kamhub.tar.gz -C kamhub
cd kamhub
chmod +x deploy-kamhub.sh
./deploy-kamhub.sh
```

---

## 🎯 ЧТО ВЫБИРАЕТЕ?

**Вариант А:** Создать GitHub репозиторий (рекомендую) - потом легко обновлять  
**Вариант Б:** Ручная загрузка через SCP

Скажите, какой вариант выбираете, или дайте ссылку на репозиторий, если уже создали!

