# 🚀 Быстрый деплой исправлений регистрации партнеров

## Что исправлено?

- ✅ Таблица `users` получила поле `password_hash`
- ✅ Таблица `partners` получила поле `user_id` для связи с users
- ✅ API `/api/partners/register` полностью переписан с хешированием паролей
- ✅ Добавлена автоматическая авторизация через JWT
- ✅ Все операции в транзакциях

## 3 способа применить исправления:

### ⚡ Способ 1: Автоматический скрипт (РЕКОМЕНДУЕТСЯ)

```bash
# На сервере Timeweb
ssh root@147.45.102.76
# Пароль: cLE8n-fH+QM5@U

cd /path/to/kamhub

# Установите DATABASE_URL если не установлен
export DATABASE_URL="postgresql://user:password@host:port/database"

# Запустите скрипт
bash deploy-partner-fix.sh
```

### 🔧 Способ 2: Пошаговый

```bash
# 1. Установите зависимости
npm install

# 2. Примените миграции
npm run db:migrate

# 3. Соберите приложение
npm run build

# 4. Перезапустите
pm2 restart kamhub
# или
npm start
```

### 📝 Способ 3: Вручную через psql

```bash
# Подключитесь к PostgreSQL
psql $DATABASE_URL

# Выполните миграцию 006
\i lib/database/migrations/006_add_password_hash_to_users.sql

# Выполните миграцию 007
\i lib/database/migrations/007_add_user_id_to_partners.sql

# Проверьте
\d users
\d partners
```

## Проверка после деплоя:

### 1. Проверьте структуру БД
```sql
-- Должно показать password_hash
\d users

-- Должно показать user_id и внешний ключ
\d partners
```

### 2. Протестируйте API
```bash
curl -X POST http://your-domain.com/api/partners/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовая компания",
    "email": "test@example.com",
    "phone": "+7 999 123 4567",
    "password": "test12345678",
    "roles": ["operator"]
  }'
```

Ожидаемый ответ:
```json
{
  "success": true,
  "message": "Партнер успешно зарегистрирован!",
  "data": {
    "user": { "id": "...", "email": "...", "name": "..." },
    "partnerIds": ["..."],
    "roles": ["operator"]
  }
}
```

### 3. Откройте в браузере
```
http://your-domain.com/partner/register
```

Заполните форму и проверьте:
- ✅ Форма отправляется без ошибок
- ✅ Появляется сообщение "Успешно!"
- ✅ Происходит перенаправление на dashboard
- ✅ Пользователь автоматически авторизован

## Откат (если что-то пошло не так):

```sql
-- Откатить миграцию 007
DROP INDEX IF EXISTS idx_partners_user_category;
DROP INDEX IF EXISTS idx_partners_user_id;
ALTER TABLE partners DROP CONSTRAINT IF EXISTS fk_partners_user_id;
ALTER TABLE partners DROP COLUMN IF EXISTS user_id;

-- Откатить миграцию 006
DROP INDEX IF EXISTS idx_users_email_password;
ALTER TABLE users DROP COLUMN IF EXISTS password_hash;
```

## Контакты Timeweb:

- **SSH**: `root@147.45.102.76`
- **Пароль**: `cLE8n-fH+QM5@U`
- **S3 Storage**: https://s3.twcstorage.ru
- **Token**: (см. в переменных окружения)

## Документация:

- 📄 **Полная инструкция**: `PARTNER_REGISTRATION_FIX_INSTRUCTIONS.md`
- 📊 **Отчет о проблемах**: `PARTNER_REGISTRATION_ISSUES_REPORT.md`
- ✅ **Финальный отчет**: `PARTNER_REGISTRATION_FIXED.md`

## Если возникли проблемы:

1. Проверьте логи: `pm2 logs kamhub`
2. Проверьте DATABASE_URL: `echo $DATABASE_URL`
3. Проверьте подключение к БД: `psql $DATABASE_URL -c "SELECT 1"`
4. Проверьте версию Node.js: `node --version` (должна быть >= 18)

---

**Время деплоя**: ~5-10 минут  
**Риск**: Низкий (все операции в транзакциях)  
**Downtime**: Нет (hot reload)
