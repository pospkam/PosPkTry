# 🎁🎒 АНАЛИЗ МОДУЛЕЙ: СУВЕНИРЫ И АРЕНДА СНАРЯЖЕНИЯ

**Дата анализа:** 5 ноября 2025  
**Статус:** 🟡 **НАЧАЛЬНАЯ СТАДИЯ (20% готовности)**

---

## 📊 КРАТКОЕ РЕЗЮМЕ

Модули **Сувениры** и **Аренда снаряжения** находятся в начальной стадии разработки. Базовая инфраструктура готова (API, БД, типы), но отсутствует детальная функциональность и UI компоненты.

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║  📦 СУВЕНИРЫ (souvenirs)         [████░░░░░░░░] 20%      ║
║  🎒 АРЕНДА СНАРЯЖЕНИЯ (gear)     [████░░░░░░░░] 20%      ║
║                                                           ║
║  Статус: Базовая инфраструктура готова                   ║
║         Требуется детальная разработка                   ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
```

---

## ✅ ЧТО РЕАЛИЗОВАНО

### 1. 🏗️ Хабы (UI страницы)

**Расположение:**
- `app/hub/souvenirs/page.tsx` — Хаб сувениров
- `app/hub/gear/page.tsx` — Хаб аренды снаряжения

**Что есть:**
```typescript
// app/hub/souvenirs/page.tsx
'use client';

export default function SouvenirsHub() {
  return (
    <Protected roles={['tourist', 'admin']}>
      <main className="min-h-screen bg-premium-black text-white">
        <div className="bg-white/5 border-b border-white/10 p-6">
          <h1 className="text-3xl font-black text-premium-gold">
            Сувениры
          </h1>
          <p className="text-white/70">
            Камчатские сувениры и подарки
          </p>
        </div>
        <div className="p-6">
          <div className="bg-white/5 border border-white/10 rounded-2xl p-8 text-center">
            <div className="text-4xl mb-4">🎁</div>
            <p className="text-white/70">
              Каталог сувениров в разработке
            </p>
          </div>
        </div>
      </main>
    </Protected>
  );
}
```

**Аналогично для gear:**
```typescript
// app/hub/gear/page.tsx
<h1>Прокат снаряжения</h1>
<p>Снаряжение для активного отдыха</p>
<div className="text-4xl mb-4">🎒</div>
<p>Каталог снаряжения в разработке</p>
```

**Статус:** ✅ Базовые страницы созданы (заглушки)

---

### 2. 🔌 API Endpoints

**Реализация через общий API партнёров:**

#### `GET /api/partners`

Получение списка партнёров с фильтрацией по категории:

```typescript
// Получить сувенирные магазины
GET /api/partners?category=souvenir

// Получить пункты проката снаряжения
GET /api/partners?category=gear
```

**Параметры запроса:**
```typescript
{
  page?: number;          // Номер страницы (по умолчанию 1)
  limit?: number;         // Количество на странице (по умолчанию 10)
  category?: string;      // 'souvenir' или 'gear'
  verified?: boolean;     // Только проверенные
  search?: string;        // Поиск по названию/описанию
  sortBy?: string;        // Сортировка (по умолчанию 'created_at')
  sortOrder?: 'asc'|'desc'; // Порядок сортировки
}
```

**Пример ответа:**
```typescript
{
  success: true,
  data: {
    data: [
      {
        id: "uuid",
        name: "Камчатские сувениры",
        category: "souvenir",
        description: "Сувениры ручной работы",
        contact: {
          phone: "+7 (999) 123-45-67",
          email: "shop@example.com",
          address: "ул. Ленинская, 1"
        },
        rating: 4.8,
        reviewCount: 125,
        isVerified: true,
        logo: { url: "..." },
        images: [{ url: "..." }],
        createdAt: "2024-01-01T00:00:00Z",
        updatedAt: "2024-01-01T00:00:00Z"
      }
    ],
    pagination: {
      page: 1,
      limit: 10,
      total: 5,
      totalPages: 1
    }
  }
}
```

#### `POST /api/partners`

Создание нового партнёра (магазин/прокат):

```typescript
POST /api/partners

Body:
{
  name: "Камчатские сувениры",
  category: "souvenir", // или "gear"
  description: "Описание",
  contact: {
    phone: "+7 (999) 123-45-67",
    email: "shop@example.com",
    address: "Адрес"
  },
  isVerified: false,
  logoUrl?: "https://...",
  images?: ["https://..."]
}
```

**Валидация категорий:**
```typescript
const validCategories = [
  'operator',
  'guide',
  'transfer',
  'stay',
  'souvenir',  // ✅ Сувениры
  'gear',      // ✅ Снаряжение
  'cars',
  'restaurant'
];
```

**Файл:** `app/api/partners/route.ts`  
**Статус:** ✅ Базовый API готов

---

### 3. 🗄️ База данных

#### Таблица `partners`

```sql
CREATE TABLE IF NOT EXISTS partners (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(255) NOT NULL,
    category VARCHAR(50) NOT NULL CHECK (
        category IN (
            'operator',
            'guide',
            'transfer',
            'stay',
            'souvenir',  -- ✅ Сувениры
            'gear',      -- ✅ Снаряжение
            'cars',
            'restaurant'
        )
    ),
    description TEXT,
    contact JSONB NOT NULL,
    rating DECIMAL(3,2) DEFAULT 0.0,
    review_count INTEGER DEFAULT 0,
    is_verified BOOLEAN DEFAULT FALSE,
    logo_asset_id UUID,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

**Связанные таблицы:**
- `partner_assets` — Изображения партнёра
- `assets` — Файлы/изображения

**Файл:** `lib/database/schema.sql`  
**Статус:** ✅ Базовая схема готова

---

### 4. 📝 TypeScript типы

```typescript
// types/index.ts

export interface Partner {
  id: string;
  name: string;
  category: 'operator' | 'guide' | 'transfer' | 'stay' | 
            'souvenir' | 'gear' | 'cars' | 'restaurant';  // ✅
  description: string;
  contact: ContactInfo;
  rating: number;
  reviewCount: number;
  isVerified: boolean;
  logo?: Asset;
  images: Asset[];
  createdAt: Date;
  updatedAt: Date;
}

export interface ContactInfo {
  phone: string;
  email: string;
  address?: string;
  website?: string;
  socialMedia?: {
    vk?: string;
    telegram?: string;
    instagram?: string;
  };
}
```

**Статус:** ✅ Типы определены

---

## ❌ ЧТО НЕ РЕАЛИЗОВАНО

### 1. 📦 Товары/Услуги

**Проблема:** Нет отдельных сущностей для товаров и услуг

**Что нужно:**

#### Для сувениров:

```typescript
interface SouvenirProduct {
  id: string;
  partnerId: string;        // Магазин
  name: string;             // Название товара
  description: string;
  category: string;         // Категория: магниты, одежда, керамика, и т.д.
  price: number;
  currency: string;
  images: Asset[];
  inStock: boolean;
  stockQuantity?: number;
  weight?: number;          // Вес (для доставки)
  dimensions?: {            // Размеры
    length: number;
    width: number;
    height: number;
  };
  materials?: string[];     // Материалы
  madeIn?: string;          // Произведено
  handmade: boolean;        // Ручная работа
  tags: string[];           // Теги для поиска
  createdAt: Date;
  updatedAt: Date;
}
```

**SQL схема:**
```sql
CREATE TABLE souvenirs (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    partner_id UUID REFERENCES partners(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    category VARCHAR(100),
    price DECIMAL(10,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    in_stock BOOLEAN DEFAULT TRUE,
    stock_quantity INTEGER,
    weight DECIMAL(10,2),
    dimensions JSONB,
    materials TEXT[],
    made_in VARCHAR(100),
    handmade BOOLEAN DEFAULT FALSE,
    tags TEXT[],
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_souvenirs_partner ON souvenirs(partner_id);
CREATE INDEX idx_souvenirs_category ON souvenirs(category);
CREATE INDEX idx_souvenirs_in_stock ON souvenirs(in_stock);
```

#### Для аренды снаряжения:

```typescript
interface GearItem {
  id: string;
  partnerId: string;        // Прокатная компания
  name: string;             // Название снаряжения
  description: string;
  category: string;         // Категория: палатки, рюкзаки, спальники, и т.д.
  pricePerDay: number;      // Цена за день
  pricePerWeek?: number;    // Цена за неделю (скидка)
  deposit: number;          // Залог
  currency: string;
  condition: 'new' | 'good' | 'used';
  images: Asset[];
  available: boolean;
  totalQuantity: number;    // Всего единиц
  availableQuantity: number; // Свободно
  size?: string;            // Размер (для одежды)
  specifications: Record<string, any>; // Технические характеристики
  included: string[];       // Что включено в аренду
  recommendations?: string; // Рекомендации по использованию
  createdAt: Date;
  updatedAt: Date;
}
```

**SQL схема:**
```sql
CREATE TABLE gear_items (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    partner_id UUID REFERENCES partners(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    category VARCHAR(100),
    price_per_day DECIMAL(10,2) NOT NULL,
    price_per_week DECIMAL(10,2),
    deposit DECIMAL(10,2) NOT NULL,
    currency VARCHAR(3) DEFAULT 'RUB',
    condition VARCHAR(20) CHECK (condition IN ('new', 'good', 'used')),
    available BOOLEAN DEFAULT TRUE,
    total_quantity INTEGER NOT NULL,
    available_quantity INTEGER NOT NULL,
    size VARCHAR(50),
    specifications JSONB,
    included TEXT[],
    recommendations TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_gear_partner ON gear_items(partner_id);
CREATE INDEX idx_gear_category ON gear_items(category);
CREATE INDEX idx_gear_available ON gear_items(available);
```

**Статус:** ❌ НЕ РЕАЛИЗОВАНО

---

### 2. 🛒 Корзина и заказы

**Что нужно:**

```typescript
// Корзина для сувениров
interface SouvenirCartItem {
  productId: string;
  quantity: number;
  price: number;
}

interface SouvenirOrder {
  id: string;
  userId: string;
  items: SouvenirCartItem[];
  subtotal: number;
  tax: number;
  shipping: number;
  total: number;
  shippingAddress: Address;
  status: 'pending' | 'paid' | 'shipped' | 'delivered' | 'cancelled';
  paymentMethod: string;
  paymentId?: string;
  trackingNumber?: string;
  createdAt: Date;
  updatedAt: Date;
}

// Бронирование снаряжения
interface GearRental {
  id: string;
  userId: string;
  gearItemId: string;
  quantity: number;
  startDate: Date;
  endDate: Date;
  days: number;
  pricePerDay: number;
  totalPrice: number;
  deposit: number;
  status: 'pending' | 'confirmed' | 'active' | 'returned' | 'cancelled';
  pickupLocation: string;
  returnLocation: string;
  notes?: string;
  paymentId?: string;
  createdAt: Date;
  updatedAt: Date;
}
```

**Статус:** ❌ НЕ РЕАЛИЗОВАНО

---

### 3. 🎨 UI Компоненты

**Что нужно создать:**

#### Для сувениров:

```typescript
// components/souvenirs/SouvenirCard.tsx
interface SouvenirCardProps {
  product: SouvenirProduct;
  onAddToCart: (productId: string) => void;
}

// components/souvenirs/SouvenirFilters.tsx
// Фильтры: категория, цена, материалы, ручная работа

// components/souvenirs/SouvenirGrid.tsx
// Сетка товаров с пагинацией

// components/souvenirs/SouvenirDetail.tsx
// Детальная страница товара

// components/souvenirs/ShoppingCart.tsx
// Корзина покупок

// components/souvenirs/CheckoutForm.tsx
// Форма оформления заказа
```

#### Для аренды снаряжения:

```typescript
// components/gear/GearCard.tsx
interface GearCardProps {
  item: GearItem;
  onRent: (itemId: string) => void;
}

// components/gear/GearFilters.tsx
// Фильтры: категория, цена, состояние, размер

// components/gear/GearCalendar.tsx
// Календарь доступности

// components/gear/GearRentalForm.tsx
// Форма бронирования снаряжения

// components/gear/RentalList.tsx
// Список моих бронирований
```

**Статус:** ❌ НЕ РЕАЛИЗОВАНО

---

### 4. 🔌 Специфичные API

**Что нужно создать:**

#### Для сувениров:

```typescript
// app/api/souvenirs/route.ts
GET    /api/souvenirs              // Список товаров
GET    /api/souvenirs/[id]         // Детали товара
POST   /api/souvenirs              // Создать товар (партнёр)
PUT    /api/souvenirs/[id]         // Обновить товар
DELETE /api/souvenirs/[id]         // Удалить товар

// app/api/souvenirs/cart/route.ts
GET    /api/souvenirs/cart         // Получить корзину
POST   /api/souvenirs/cart         // Добавить в корзину
DELETE /api/souvenirs/cart/[id]    // Удалить из корзины

// app/api/souvenirs/orders/route.ts
GET    /api/souvenirs/orders       // Список заказов
POST   /api/souvenirs/orders       // Создать заказ
GET    /api/souvenirs/orders/[id]  // Детали заказа
PATCH  /api/souvenirs/orders/[id]  // Обновить статус
```

#### Для аренды снаряжения:

```typescript
// app/api/gear/route.ts
GET    /api/gear                   // Список снаряжения
GET    /api/gear/[id]              // Детали снаряжения
POST   /api/gear                   // Создать снаряжение (партнёр)
PUT    /api/gear/[id]              // Обновить снаряжение
DELETE /api/gear/[id]              // Удалить снаряжение

// app/api/gear/availability/route.ts
GET    /api/gear/[id]/availability // Проверить доступность

// app/api/gear/rentals/route.ts
GET    /api/gear/rentals           // Список бронирований
POST   /api/gear/rentals           // Создать бронирование
GET    /api/gear/rentals/[id]      // Детали бронирования
PATCH  /api/gear/rentals/[id]      // Обновить статус
DELETE /api/gear/rentals/[id]      // Отменить бронирование
```

**Статус:** ❌ НЕ РЕАЛИЗОВАНО

---

### 5. 📱 Страницы приложения

**Что нужно создать:**

#### Для сувениров:

```
app/souvenirs/
├── page.tsx                    // Каталог сувениров
├── [id]/
│   └── page.tsx                // Детальная страница товара
├── cart/
│   └── page.tsx                // Корзина
├── checkout/
│   └── page.tsx                // Оформление заказа
└── orders/
    ├── page.tsx                // Список заказов
    └── [id]/
        └── page.tsx            // Детали заказа
```

#### Для аренды снаряжения:

```
app/gear/
├── page.tsx                    // Каталог снаряжения
├── [id]/
│   └── page.tsx                // Детальная страница снаряжения
├── rental/
│   └── page.tsx                // Форма бронирования
└── my-rentals/
    ├── page.tsx                // Мои бронирования
    └── [id]/
        └── page.tsx            // Детали бронирования
```

**Статус:** ❌ НЕ РЕАЛИЗОВАНО (есть только хабы с заглушками)

---

## 📊 ТЕКУЩИЙ СТАТУС

```
╔═══════════════════════════════════════════════════════════╗
║  ЧТО ЕСТЬ                                                 ║
╠═══════════════════════════════════════════════════════════╣
║  ✅ Хабы (заглушки)                  [████████████] 100%  ║
║  ✅ API partners (базовый)           [████████████] 100%  ║
║  ✅ БД схема partners                [████████████] 100%  ║
║  ✅ TypeScript типы Partner          [████████████] 100%  ║
╠═══════════════════════════════════════════════════════════╣
║  ЧТО ОТСУТСТВУЕТ                                          ║
╠═══════════════════════════════════════════════════════════╣
║  ❌ Схема БД для товаров/услуг       [░░░░░░░░░░░░] 0%   ║
║  ❌ TypeScript типы товаров          [░░░░░░░░░░░░] 0%   ║
║  ❌ API для товаров                  [░░░░░░░░░░░░] 0%   ║
║  ❌ UI компоненты                    [░░░░░░░░░░░░] 0%   ║
║  ❌ Корзина/Заказы                   [░░░░░░░░░░░░] 0%   ║
║  ❌ Бронирование снаряжения          [░░░░░░░░░░░░] 0%   ║
║  ❌ Интеграция с платежами           [░░░░░░░░░░░░] 0%   ║
╠═══════════════════════════════════════════════════════════╣
║  ОБЩАЯ ГОТОВНОСТЬ:                   [████░░░░░░░░] 20%  ║
╚═══════════════════════════════════════════════════════════╝
```

---

## 🎯 ПЛАН РАЗРАБОТКИ

### Этап 1: База данных (3-4 часа)

**Приоритет:** 🔴 ВЫСОКИЙ

1. **Создать схему БД для товаров:**
   ```sql
   -- lib/database/souvenirs_schema.sql
   CREATE TABLE souvenirs (...);
   CREATE TABLE souvenir_assets (...);
   
   -- lib/database/gear_schema.sql
   CREATE TABLE gear_items (...);
   CREATE TABLE gear_assets (...);
   CREATE TABLE gear_rentals (...);
   ```

2. **Создать миграции:**
   ```bash
   npm run migrate:up
   ```

**Оценка:** 3-4 часа

---

### Этап 2: TypeScript типы (1 час)

**Приоритет:** 🔴 ВЫСОКИЙ

1. **Создать типы:**
   ```typescript
   // types/souvenirs.ts
   export interface SouvenirProduct { ... }
   export interface SouvenirOrder { ... }
   
   // types/gear.ts
   export interface GearItem { ... }
   export interface GearRental { ... }
   ```

**Оценка:** 1 час

---

### Этап 3: API endpoints (8-10 часов)

**Приоритет:** 🟡 СРЕДНИЙ

1. **Сувениры API (4-5 часов):**
   - GET/POST /api/souvenirs
   - GET/POST /api/souvenirs/cart
   - GET/POST /api/souvenirs/orders

2. **Снаряжение API (4-5 часов):**
   - GET/POST /api/gear
   - GET /api/gear/availability
   - GET/POST /api/gear/rentals

**Оценка:** 8-10 часов

---

### Этап 4: UI компоненты (12-15 часов)

**Приоритет:** 🟡 СРЕДНИЙ

1. **Сувениры UI (6-7 часов):**
   - SouvenirCard
   - SouvenirFilters
   - SouvenirGrid
   - ShoppingCart
   - CheckoutForm

2. **Снаряжение UI (6-8 часов):**
   - GearCard
   - GearFilters
   - GearCalendar
   - GearRentalForm
   - RentalList

**Оценка:** 12-15 часов

---

### Этап 5: Страницы (8-10 часов)

**Приоритет:** 🟡 СРЕДНИЙ

1. **Сувениры страницы (4-5 часов):**
   - Каталог
   - Детальная страница
   - Корзина
   - Оформление заказа

2. **Снаряжение страницы (4-5 часов):**
   - Каталог
   - Детальная страница
   - Форма бронирования
   - Мои бронирования

**Оценка:** 8-10 часов

---

### Этап 6: Интеграции (4-6 часов)

**Приоритет:** 🟢 НИЗКИЙ

1. **CloudPayments:**
   - Оплата заказов сувениров
   - Оплата аренды снаряжения

2. **Email уведомления:**
   - Подтверждение заказа
   - Подтверждение бронирования

**Оценка:** 4-6 часов

---

### Итого: 36-46 часов (~1 неделя работы)

```
Этап 1: БД               [3-4ч]   █████░░░░░░░░░░
Этап 2: Типы             [1ч]     ██░░░░░░░░░░░░░
Этап 3: API              [8-10ч]  ████████████████
Этап 4: UI               [12-15ч] ██████████████████████████
Этап 5: Страницы         [8-10ч]  ████████████████
Этап 6: Интеграции       [4-6ч]   ████████░░░░░░░
─────────────────────────────────────────────────
ИТОГО:                   36-46 часов
```

---

## 💡 РЕКОМЕНДАЦИИ

### 🔴 Критично:

1. **Определить приоритет модулей**
   - Какой модуль важнее: сувениры или снаряжение?
   - Или разрабатывать параллельно?

2. **Уточнить требования:**
   - Нужна ли доставка сувениров?
   - Нужна ли система отзывов?
   - Нужна ли система скидок/промокодов?

### 🟡 Важно:

3. **Использовать существующие решения:**
   - Можно адаптировать логику из модуля accommodations
   - Можно переиспользовать календарь из системы бронирований

4. **Интеграция с остальной системой:**
   - Программа лояльности (начисление баллов)
   - Эко-баллы (экологичные товары)
   - AI рекомендации (персонализация)

---

## 🎊 ЗАКЛЮЧЕНИЕ

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║  МОДУЛИ СУВЕНИРЫ И СНАРЯЖЕНИЕ                            ║
║                                                           ║
║  Текущий статус:     20% готовности                      ║
║  Базовая инфра:      ✅ Готова                           ║
║  Детальная функц:    ❌ Отсутствует                      ║
║                                                           ║
║  Время до готовности: 36-46 часов (~1 неделя)            ║
║                                                           ║
║  Рекомендация:       Начать с БД и типов,                ║
║                      затем API, затем UI                 ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝
```

---

**Дата анализа:** 5 ноября 2025  
**Аналитик:** AI Assistant  
**Файлов проанализировано:** 10+

🏔️ **Сделано с любовью для Камчатки!** 🏔️




