# 🚀 Деплой KamHub на Timeweb Cloud

**Официальный сайт:** https://timeweb.cloud/  
**Дата:** 5 ноября 2025

---

## 🎯 ПОЧЕМУ TIMEWEB CLOUD?

✅ **Топ-1** по ценам среди IaaS в России  
✅ **99,98%** гарантированный аптайм (SLA)  
✅ **17 дата-центров** по всему миру  
✅ **PostgreSQL** в облаке (готовые СУБД)  
✅ **VDS от 199₽/мес** - идеально для старта  
✅ **Поддержка 24/7** на русском языке  

**Рекомендуется для проектов на российском рынке!**

---

## 📋 ЧТО НУЖНО

### Минимальная конфигурация (для старта):

**VDS сервер:**
- CPU: 2 vCPU
- RAM: 4 GB
- Disk: 40 GB SSD
- OS: Ubuntu 22.04
- **Цена:** ~700₽/месяц

**Облачная БД PostgreSQL 15:**
- RAM: 2 GB
- Disk: 10 GB
- **Цена:** ~500₽/месяц

**Итого:** ~1,200₽/месяц для полноценного проекта

---

## 🚀 БЫСТРЫЙ СТАРТ (30 минут)

### Шаг 1: Создайте аккаунт Timeweb Cloud

1. Перейдите на https://timeweb.cloud/
2. Нажмите **"Создать аккаунт"**
3. Подтвердите email
4. Пополните баланс (минимум 300₽)

### Шаг 2: Создайте базу данных PostgreSQL

1. В панели Timeweb → **"Облачные базы данных"**
2. Нажмите **"Создать БД"**
3. Выберите:
   - **Тип:** PostgreSQL 15
   - **Конфигурация:** 2 GB RAM, 10 GB Disk
   - **Локация:** Москва (или ближайший)
4. Нажмите **"Создать"**
5. **Сохраните данные подключения:**
   ```
   Host: xxxxx.timeweb.cloud
   Port: 5432
   Database: kamhub
   User: gen_user
   Password: [сгенерированный пароль]
   ```

6. **Включите PostGIS расширение:**
   - Подключитесь к БД через веб-консоль
   - Выполните: `CREATE EXTENSION IF NOT EXISTS postgis;`

### Шаг 3: Создайте VDS сервер

1. В панели Timeweb → **"VDS и VPS"**
2. Нажмите **"Создать сервер"**
3. Выберите конфигурацию:
   ```
   CPU: 2 vCPU
   RAM: 4 GB
   SSD: 40 GB
   OS: Ubuntu 22.04 LTS
   Локация: Москва
   ```
4. Нажмите **"Создать сервер"**
5. **Сохраните данные доступа:**
   ```
   IP: XXX.XXX.XXX.XXX
   User: root
   Password: [сгенерированный]
   ```

### Шаг 4: Подключитесь к серверу

**Windows (PowerShell):**
```powershell
ssh root@XXX.XXX.XXX.XXX
# Введите пароль
```

**Или используйте веб-консоль Timeweb:**
1. В панели управления → нажмите на сервер
2. Кнопка **"Консоль"** → откроется терминал в браузере

### Шаг 5: Автоматическая установка

```bash
# 1. Обновите систему
apt update && apt upgrade -y

# 2. Скачайте скрипт установки
curl -o setup.sh https://raw.githubusercontent.com/YOUR_REPO/kamhub/main/scripts/setup-timeweb-server.sh

# 3. Сделайте его исполняемым
chmod +x setup.sh

# 4. Запустите установку
bash setup.sh
```

**Скрипт автоматически установит:**
- ✅ Node.js 20
- ✅ PM2 (менеджер процессов)
- ✅ Nginx (веб-сервер)
- ✅ Git
- ✅ Certbot (для SSL)

### Шаг 6: Настройте проект

```bash
# 1. Клонируйте репозиторий
cd /var/www
git clone https://github.com/YOUR_REPO/kamhub.git
cd kamhub

# 2. Установите зависимости
npm install

# 3. Создайте .env файл
nano .env
```

**Вставьте конфигурацию:**
```env
# База данных (из Шага 2)
DATABASE_URL=postgresql://gen_user:PASSWORD@xxxxx.timeweb.cloud:5432/kamhub

# AI API ключи (получите бесплатно)
GROQ_API_KEY=gsk_your_key_here
DEEPSEEK_API_KEY=sk_your_key_here
OPENROUTER_API_KEY=sk-or_your_key_here

# Yandex Maps (бесплатно до 25k req/день)
YANDEX_MAPS_API_KEY=your_key

# CloudPayments (для приема платежей)
CLOUDPAYMENTS_PUBLIC_ID=pk_your_id
CLOUDPAYMENTS_API_SECRET=your_secret

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=$(openssl rand -base64 32)

# URL вашего сервера
NEXT_PUBLIC_APP_URL=http://XXX.XXX.XXX.XXX
```

**Сохраните:** `Ctrl+X`, `Y`, `Enter`

### Шаг 7: Примените миграции БД

```bash
# Проверьте подключение к БД
npm run db:test

# Примените миграции
npm run migrate:up

# Проверьте статус
npm run migrate:status
```

### Шаг 8: Соберите проект

```bash
# Сборка для production
npm run build

# Это займет 2-5 минут
```

### Шаг 9: Запустите через PM2

```bash
# Запустите приложение
pm2 start ecosystem.config.js

# Сохраните конфигурацию
pm2 save

# Настройте автозапуск
pm2 startup
# Выполните команду, которую покажет PM2

# Проверьте статус
pm2 status
pm2 logs kamhub
```

### Шаг 10: Настройте Nginx

```bash
# Создайте конфигурацию
nano /etc/nginx/sites-available/kamhub
```

**Вставьте:**
```nginx
server {
    listen 80;
    server_name XXX.XXX.XXX.XXX;  # Замените на ваш IP

    location / {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Увеличиваем таймауты для AI запросов
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
}
```

```bash
# Включите конфиг
ln -s /etc/nginx/sites-available/kamhub /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl restart nginx
```

### Шаг 11: Проверьте работу!

```bash
# Откройте в браузере
http://XXX.XXX.XXX.XXX

# Должна открыться главная страница KamHub!
```

---

## 🌐 НАСТРОЙКА ДОМЕНА (опционально)

### Вариант 1: Домен от Timeweb

1. В панели Timeweb → **"Домены"**
2. Купите домен или перенесите существующий
3. В настройках домена → **DNS записи**
4. Добавьте A-запись:
   ```
   Тип: A
   Имя: @
   Значение: XXX.XXX.XXX.XXX (IP вашего сервера)
   TTL: 3600
   ```

### Вариант 2: Внешний регистратор

Если домен куплен в другом месте:

1. В панели регистратора домена
2. Добавьте A-запись на IP вашего сервера
3. Подождите 1-24 часа (propagation)

### Установка SSL сертификата (бесплатно!)

После привязки домена:

```bash
# Установите Certbot
apt install certbot python3-certbot-nginx -y

# Получите сертификат
certbot --nginx -d your-domain.ru

# Введите email для уведомлений
# Согласитесь с условиями

# Certbot автоматически настроит Nginx и SSL!
```

**Теперь ваш сайт доступен по HTTPS!** 🔒

---

## 📊 МОНИТОРИНГ И УПРАВЛЕНИЕ

### Просмотр логов

```bash
# Логи приложения
pm2 logs kamhub

# Только последние ошибки
pm2 logs kamhub --err

# Логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### Мониторинг ресурсов

```bash
# PM2 monitoring (интерактивный)
pm2 monit

# Статус процессов
pm2 status

# Использование памяти/CPU
htop
```

### Через панель Timeweb Cloud

1. Откройте панель управления
2. Выберите ваш VDS сервер
3. Вкладка **"Мониторинг"**
   - CPU использование
   - RAM использование
   - Disk I/O
   - Network трафик

---

## 🔄 ОБНОВЛЕНИЕ ПРОЕКТА

```bash
# Подключитесь к серверу
ssh root@XXX.XXX.XXX.XXX

# Перейдите в папку проекта
cd /var/www/kamhub

# Получите последние изменения
git pull origin main

# Установите новые зависимости (если есть)
npm install

# Примените новые миграции БД (если есть)
npm run migrate:up

# Пересоберите проект
npm run build

# Перезапустите PM2
pm2 reload kamhub

# Проверьте статус
pm2 status
pm2 logs kamhub --lines 50
```

---

## 💾 НАСТРОЙКА BACKUP

### Автоматический backup базы данных

```bash
# Создайте скрипт backup
nano /root/backup-kamhub.sh
```

**Вставьте:**
```bash
#!/bin/bash

# Настройки
DB_HOST="xxxxx.timeweb.cloud"
DB_PORT="5432"
DB_NAME="kamhub"
DB_USER="gen_user"
DB_PASS="your_password"
BACKUP_DIR="/root/backups"
DATE=$(date +%Y%m%d_%H%M%S)

# Создайте папку для backup
mkdir -p $BACKUP_DIR

# Создайте backup
PGPASSWORD=$DB_PASS pg_dump -h $DB_HOST -p $DB_PORT -U $DB_USER -d $DB_NAME > $BACKUP_DIR/kamhub_$DATE.sql

# Удалите старые backup (старше 7 дней)
find $BACKUP_DIR -name "kamhub_*.sql" -mtime +7 -delete

echo "Backup completed: kamhub_$DATE.sql"
```

```bash
# Сделайте скрипт исполняемым
chmod +x /root/backup-kamhub.sh

# Добавьте в cron (каждый день в 3:00)
crontab -e

# Добавьте строку:
0 3 * * * /root/backup-kamhub.sh >> /var/log/backup.log 2>&1
```

### Backup через панель Timeweb

1. В панели управления БД
2. Вкладка **"Резервные копии"**
3. Настройте автоматические backup
4. Выберите расписание и срок хранения

---

## 🔥 МАСШТАБИРОВАНИЕ

### Увеличение ресурсов сервера

**Через панель Timeweb:**
1. Выберите ваш VDS сервер
2. Нажмите **"Изменить конфигурацию"**
3. Выберите новую конфигурацию:
   - 4 vCPU, 8 GB RAM (~1,500₽/мес)
   - 6 vCPU, 16 GB RAM (~3,000₽/мес)
4. Сервер перезагрузится с новыми ресурсами

**Без простоя:** Создайте снимок → Разверните на новом сервере → Переключите домен

### Балансировщик нагрузки

Для высоконагруженных проектов:

1. Создайте 2-3 VDS сервера
2. Настройте **Балансировщик Timeweb Cloud**
3. Распределите нагрузку между серверами
4. Получите горизонтальное масштабирование!

---

## 🛡️ БЕЗОПАСНОСТЬ

### Настройка файрвола

```bash
# Установите UFW
apt install ufw -y

# Разрешите SSH
ufw allow 22/tcp

# Разрешите HTTP/HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включите файрвол
ufw enable

# Проверьте статус
ufw status
```

### Через панель Timeweb

1. В настройках VDS → **"Сеть"**
2. **"Firewall"** → Настройте правила
3. Разрешите только нужные порты

### Дополнительная защита

```bash
# Установите Fail2Ban (защита от брутфорса)
apt install fail2ban -y

# Запустите
systemctl enable fail2ban
systemctl start fail2ban
```

---

## 💰 ОПТИМИЗАЦИЯ ЗАТРАТ

### Текущие расходы:

| Сервис | Конфигурация | Цена/мес |
|--------|-------------|----------|
| VDS | 2 vCPU, 4 GB RAM | ~700₽ |
| PostgreSQL | 2 GB RAM, 10 GB | ~500₽ |
| **Итого** | | **~1,200₽** |

### Экономия:

**Используйте промокоды Timeweb:**
- При регистрации
- На форумах и в соцсетях
- **Можно получить скидку до 50%!**

**Оплата на год вперед:**
- Скидка 10-20%
- ~11,000₽/год вместо 14,400₽

**Бесплатные альтернативы для разработки:**
- GROQ API: бесплатно 14k req/день
- Yandex Maps: бесплатно 25k req/день
- Open-Meteo: полностью бесплатно

---

## 🧪 ТЕСТИРОВАНИЕ

### Health Checks

```bash
# API Health
curl http://XXX.XXX.XXX.XXX/api/health

# Database Health
curl http://XXX.XXX.XXX.XXX/api/health/db

# Туры
curl http://XXX.XXX.XXX.XXX/api/tours

# AI Chat
curl -X POST http://XXX.XXX.XXX.XXX/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message":"Привет!","sessionId":"test"}'
```

### Нагрузочное тестирование

```bash
# Установите Apache Bench
apt install apache2-utils -y

# Тест нагрузки (100 запросов, 10 одновременно)
ab -n 100 -c 10 http://XXX.XXX.XXX.XXX/
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Сервер не отвечает

```bash
# Проверьте статус PM2
pm2 status

# Если упало - перезапустите
pm2 restart kamhub

# Проверьте логи ошибок
pm2 logs kamhub --err --lines 100
```

### Проблема: Ошибка подключения к БД

```bash
# Проверьте доступность БД
psql "postgresql://user:pass@host:5432/kamhub" -c "SELECT 1"

# Проверьте переменные окружения
cat /var/www/kamhub/.env | grep DATABASE_URL

# Проверьте firewall Timeweb
# В панели БД → Настройки → Проверьте белый список IP
```

### Проблема: Медленная работа

```bash
# Проверьте загрузку CPU/RAM
htop

# Проверьте место на диске
df -h

# Очистите логи
pm2 flush
find /var/log -name "*.log" -mtime +7 -delete
```

### Получение помощи

**Техподдержка Timeweb Cloud:**
- 📞 **8 (800) 700-32-92** (бесплатно по России)
- 💬 Чат на сайте (24/7)
- 📧 support@timeweb.cloud
- 📱 Telegram: @timeweb_support

---

## 📊 МОНИТОРИНГ ПРОИЗВОДИТЕЛЬНОСТИ

### Настройка мониторинга через Timeweb

1. В панели VDS → **"Мониторинг"**
2. Настройте алерты:
   - CPU > 80%
   - RAM > 90%
   - Disk > 85%
3. Получайте уведомления на email/Telegram

### Подключение внешнего мониторинга

```bash
# Установите Node Exporter для Prometheus (опционально)
wget https://github.com/prometheus/node_exporter/releases/download/v1.6.1/node_exporter-1.6.1.linux-amd64.tar.gz
tar xvf node_exporter-1.6.1.linux-amd64.tar.gz
cd node_exporter-1.6.1.linux-amd64
./node_exporter &
```

---

## 🎉 ГОТОВО!

### Ваш KamHub работает на Timeweb Cloud! 🚀

**Проверьте:**
- ✅ http://ваш-IP или https://ваш-домен.ru
- ✅ Все страницы загружаются
- ✅ AI-чат работает
- ✅ Бронирования создаются
- ✅ Админ-панель доступна

### Следующие шаги:

1. **Создайте первого админа** в БД
2. **Добавьте реальные туры** через админку
3. **Настройте домен и SSL**
4. **Включите backup**
5. **Настройте мониторинг**
6. **Пригласите первых пользователей!**

---

## 📚 ПОЛЕЗНЫЕ ССЫЛКИ

**Timeweb Cloud:**
- 🌐 Главная: https://timeweb.cloud/
- 📖 Документация: https://timeweb.cloud/docs
- 💬 Комьюнити: https://t.me/timeweb_community
- 📊 Роадмап: https://timeweb.cloud/roadmap
- 🎓 Блог: https://timeweb.cloud/blog

**KamHub:**
- 📖 Архитектура: `docs/ARCHITECTURE_GUIDE.md`
- ✅ Статус готовности: `docs/DEPLOYMENT_READY.md`
- 🚀 Общее руководство: `DEPLOY.md`

---

**Успешного деплоя! 🎊**

*Сделано с ❤️ для Камчатки и Timeweb Cloud*



