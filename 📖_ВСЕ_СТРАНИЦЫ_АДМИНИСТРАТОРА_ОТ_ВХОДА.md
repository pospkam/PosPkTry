# 📖 ВСЕ СТРАНИЦЫ АДМИНИСТРАТОРА: ОТ ВХОДА ДО КАЖДОЙ ФУНКЦИИ

**Дата:** 27 ноября 2025  
**Роль:** Администратор  
**Всего страниц:** 8 (1 вход + 7 админ-панель)  

---

## 🎯 ОБЗОР СИСТЕМЫ

**Структура администрирования:**
- 🚪 **1 страница входа** → `/auth/login`
- 🎛️ **1 главная страница** (дашборд) → `/hub/admin`
- 📊 **6 страниц управления** → `/hub/admin/*`

**Всего компонентов:** 16  
**Всего API endpoints:** 15  
**Защита:** Только роль `admin`

---

## 📋 ОГЛАВЛЕНИЕ

1. [Страница входа](#1-страница-входа)
2. [Главная страница (Dashboard)](#2-главная-страница-dashboard)
3. [Управление пользователями](#3-управление-пользователями)
4. [Модерация туров](#4-модерация-туров)
5. [Модерация партнёров](#5-модерация-партнёров)
6. [Модерация отзывов](#6-модерация-отзывов)
7. [Финансовое управление](#7-финансовое-управление)
8. [Настройки системы](#8-настройки-системы)
9. [Навигация и защита](#9-навигация-и-защита)

---

---

# 1. СТРАНИЦА ВХОДА

## 📍 Информация

**URL:** `/auth/login`  
**Файл:** `app/auth/login/page.tsx`  
**Доступ:** Публичная (не требует авторизации)  
**Дизайн:** Samsung Weather тема

---

## 🎨 Внешний вид

### Анимированный фон
```tsx
<div className="fixed inset-0 overflow-hidden pointer-events-none">
  <div className="absolute -top-40 -right-40 w-80 h-80 bg-premium-gold/20 rounded-full blur-3xl animate-pulse"></div>
  <div className="absolute -bottom-40 -left-40 w-80 h-80 bg-blue-500/20 rounded-full blur-3xl animate-pulse delay-1000"></div>
</div>
```
**Эффект:** Плавающие градиентные сферы (золотая и синяя)

---

### Логотип и заголовок
```tsx
<img src="/logo-kamchatka.svg" alt="Kamchatka Tour Hub" />
<h1 className="text-5xl md:text-6xl font-black mb-3 bg-gradient-to-r text-white">
  Kamchatka Tour Hub
</h1>
<p className="text-xl text-white/70">
  Экосистема туризма Камчатки
</p>
```

---

## 🔄 Режимы работы

Страница имеет **2 режима**:

### 1️⃣ Режим входа (Login)
### 2️⃣ Режим регистрации (Register)

**Переключение:**
```tsx
const [mode, setMode] = useState<'login' | 'register'>('register');
```

**UI переключателя:**
```tsx
<div className="bg-white/15 backdrop-blur-2xl border border-white/15 rounded-2xl p-1.5 flex gap-2">
  <button onClick={() => setMode('login')}>Вход</button>
  <button onClick={() => setMode('register')}>Регистрация</button>
</div>
```

---

## 🔐 РЕЖИМ ВХОДА (LOGIN)

### Форма входа

**Поля:**
1. **Email** (обязательное)
   - Тип: `email`
   - Placeholder: `info@kamchatka-fishing.ru`
   - Валидация: Формат email
   
2. **Пароль** (обязательное)
   - Тип: `password` / `text` (переключаемый)
   - Placeholder: `••••••••`
   - Показать/скрыть: Кнопка глаз 👁️

**Стили полей:**
```tsx
className="w-full px-6 py-4 bg-white/15 border border-white/15 rounded-xl 
           focus:outline-none focus:ring-2 focus:ring-sky-300 
           text-white placeholder-white/40"
```

---

### Кнопка входа

```tsx
<button type="submit" disabled={loading}>
  {loading ? '⏳ Вход...' : '→ Войти'}
</button>
```

**Стили:**
```tsx
className="w-full py-4 bg-gradient-to-r from-sky-200 to-cyan-200 
           text-premium-black font-bold rounded-xl 
           hover:shadow-lg hover:shadow-blue-500/50 
           disabled:opacity-50 transition-all transform hover:scale-105"
```

---

### Логика входа

```tsx
const handleLogin = async (e: React.FormEvent) => {
  e.preventDefault();
  setLoading(true);
  setError('');

  try {
    if (loginData.email && loginData.password) {
      router.push('/partner/dashboard');
    } else {
      throw new Error('Заполните все поля');
    }
  } catch (err) {
    setError(err instanceof Error ? err.message : 'Ошибка входа');
  } finally {
    setLoading(false);
  }
};
```

**Примечание:** Текущая реализация - упрощённая (заглушка). В продакшене должна быть проверка через API:
```tsx
const response = await fetch('/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(loginData)
});
```

---

### Демо-режим

```tsx
<div className="text-center mt-6">
  <a href="/demo" className="text-blue-400 hover:text-blue-300 text-sm">
    🚀 Демо-режим (без регистрации)
  </a>
</div>
```

---

## ✍️ РЕЖИМ РЕГИСТРАЦИИ (REGISTER)

**Примечание:** Регистрация предназначена для **партнёров** (туроператоров, трансферов, размещения, снаряжения), а не для администраторов.

**Администраторы создаются напрямую в базе данных.**

---

### Секция 1: Основная информация

**Поля:**
1. **Название компании*** (required)
   - Placeholder: `Камчатская рыбалка`
   
2. **Email*** (required)
   - Type: `email`
   - Placeholder: `info@kamchatka-fishing.ru`
   
3. **Телефон*** (required)
   - Type: `tel`
   - Placeholder: `+7 (999) 123-45-67`
   
4. **Веб-сайт** (optional)
   - Type: `url`
   - Placeholder: `https://kamchatka-fishing.ru`
   
5. **Пароль*** (required)
   - Минимум 8 символов
   - Индикатор силы пароля (4 уровня)
   - Показать/скрыть
   
6. **Подтверждение пароля*** (required)
   - Валидация совпадения
   
7. **Описание компании** (optional)
   - Textarea (4 строки)
   - Placeholder: `Расскажите о вашей компании...`
   
8. **Адрес** (optional)
   - Placeholder: `г. Петропавловск-Камчатский, ул. Ленинская, 1`

---

### Секция 2: Направления деятельности

**Выбор ролей (multiple):**

| Роль | Название | Описание | Градиент |
|------|----------|----------|----------|
| `operator` | Туры | Организация туров | `from-sky-200 to-cyan-200` |
| `transfer` | Трансфер | Трансфер и доставка | `from-green-500 to-emerald-500` |
| `stay` | Размещение | Базы и гостиницы | `from-purple-500 to-pink-500` |
| `gear` | Снаряжение | Прокат снаряжения | `from-orange-500 to-red-500` |

**UI выбора:**
```tsx
<button
  onClick={() => handleRoleToggle(role.id)}
  className={`group relative p-8 rounded-2xl border-2 transition-all ${
    formData.roles.includes(role.id)
      ? 'border-transparent bg-gradient-to-br shadow-2xl scale-105'
      : 'border-white/15 bg-white/15 hover:border-white/20'
  }`}
>
  <div className="flex items-center gap-4">
    {role.icon}
    <div>
      <div className="text-2xl font-bold">{role.name}</div>
      <div className="text-sm text-white/70">{role.description}</div>
    </div>
  </div>
  
  {formData.roles.includes(role.id) && (
    <div className="mt-4">
      <span className="text-xl">✓</span> Выбрано
    </div>
  )}
</button>
```

---

### Секция 3: Логотип компании

**Загрузка файла:**
- Форматы: PNG, JPG, WEBP
- Максимальный размер: 5 МБ
- Рекомендуемый размер: 512x512px

**UI загрузки:**
```tsx
{logoPreview ? (
  <div className="w-48 h-48 rounded-2xl border-4 border-white/15 overflow-hidden">
    <img src={logoPreview} alt="Logo" />
  </div>
) : (
  <div className="w-48 h-48 rounded-2xl border-4 border-dashed border-white/20">
    <span className="text-6xl">📷</span>
    <p>Загрузите лого</p>
  </div>
)}
```

---

### Кнопки отправки

```tsx
<div className="sticky bottom-6 bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-2xl">
  <button type="button" onClick={() => router.push('/')}>
    Отмена
  </button>
  <button 
    type="submit" 
    disabled={loading || formData.roles.length === 0 || formData.password !== formData.confirmPassword}
  >
    {loading ? '⏳ Регистрация...' : '✓ Зарегистрироваться'}
  </button>
</div>
```

---

### Логика регистрации

```tsx
const handleRegister = async (e: React.FormEvent) => {
  e.preventDefault();
  setLoading(true);
  setError('');

  try {
    // Валидация
    if (formData.roles.length === 0) {
      throw new Error('Выберите хотя бы одно направление деятельности');
    }
    if (formData.password.length < 8) {
      throw new Error('Пароль должен быть не менее 8 символов');
    }
    if (formData.password !== formData.confirmPassword) {
      throw new Error('Пароли не совпадают');
    }

    // Отправка
    const response = await fetch('/api/partners/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        ...formData,
        logoUrl: logoPreview || ''
      })
    });

    const data = await response.json();

    if (!data.success) {
      throw new Error(data.error || 'Ошибка при регистрации');
    }

    // Успех
    setSuccess(true);
    setTimeout(() => {
      router.push('/partner/dashboard');
    }, 2000);

  } catch (err) {
    setError(err instanceof Error ? err.message : 'Неизвестная ошибка');
  } finally {
    setLoading(false);
  }
};
```

---

### Экран успеха

```tsx
{success && (
  <div className="min-h-screen flex items-center justify-center">
    <div className="bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-2xl rounded-3xl p-8 text-center">
      <div className="w-20 h-20 mx-auto mb-6 bg-gradient-to-br from-green-400 to-emerald-500 rounded-full animate-bounce">
        <span className="text-4xl">✓</span>
      </div>
      <h1 className="text-3xl font-bold text-white">Успешно!</h1>
      <p className="text-white/80">Ваша заявка принята</p>
      <p className="text-sm text-white/60">Перенаправление в личный кабинет...</p>
    </div>
  </div>
)}
```

---

## 🚀 КАК АДМИНИСТРАТОРУ ВОЙТИ?

**Примечание:** Текущая страница входа - для партнёров. Для администратора нужно:

### Вариант 1: Прямой URL
```
http://147.45.158.166/hub/admin
```

**Защита срабатывает через компонент `<Protected roles={['admin']}>`.**

---

### Вариант 2: Создать отдельную страницу входа для админа

**Рекомендуемый URL:** `/admin/login`

**Отличия:**
- Только Email + Пароль (без регистрации)
- Валидация через `/api/admin/auth/login`
- Перенаправление на `/hub/admin` (не `/partner/dashboard`)
- Проверка роли `admin` в базе данных

---

---

# 2. ГЛАВНАЯ СТРАНИЦА (DASHBOARD)

## 📍 Информация

**URL:** `/hub/admin`  
**Файл:** `app/hub/admin/page.tsx`  
**Защита:** `<Protected roles={['admin']}>`  
**Компоненты:**
- `AdminNav` (навигация)
- `MetricsGrid` (метрики)
- `SimpleChart` (графики)
- `RecentActivities` (последние действия)

---

## 🎯 Назначение

**Главная страница** администратора — это **центральный хаб** для мониторинга всей платформы.

**Что видит администратор:**
- 📊 Ключевые метрики (выручка, пользователи, бронирования, туры)
- 📈 Графики (выручка по месяцам, бронирования по категориям)
- 🏆 Топ туры (самые популярные)
- 📋 Последние действия (активность пользователей)
- ⚡ Быстрые действия (переход к разделам)

---

## 🎨 Структура страницы

### 1. Навигация (AdminNav)
```tsx
<AdminNav />
```
Липкая навигация вверху с кнопками:
- 📊 Dashboard
- 👥 Пользователи
- 🏔️ Туры
- 🤝 Партнёры
- 💬 Отзывы
- 💰 Финансы
- ⚙️ Настройки

---

### 2. Заголовок (Header)

```tsx
<div className="bg-white/15 border-b border-white/15 p-6">
  <div className="flex items-center justify-between">
    <div>
      <h1 className="text-3xl font-black text-white">Админ-панель</h1>
      <p className="text-white/70 mt-1">Управление платформой KamHub</p>
    </div>
    
    {/* Селектор периода */}
    <div className="flex items-center space-x-2">
      <span className="text-sm text-white/70">Период:</span>
      <select value={period} onChange={(e) => setPeriod(e.target.value)}>
        <option value="7">7 дней</option>
        <option value="30">30 дней</option>
        <option value="90">90 дней</option>
        <option value="365">Год</option>
      </select>
      <button onClick={fetchDashboardData}>🔄 Обновить</button>
    </div>
  </div>
</div>
```

**Селектор периода:**
- 7 дней
- 30 дней (по умолчанию)
- 90 дней
- Год

---

### 3. Сетка метрик (MetricsGrid)

```tsx
<MetricsGrid metrics={data.metrics} />
```

**Метрики (4 карточки):**

| Метрика | Значение | Изменение | Иконка |
|---------|----------|-----------|--------|
| **Выручка** | 1 234 567 ₽ | +12.5% | 💰 |
| **Пользователи** | 1 234 | +8.2% | 👥 |
| **Бронирования** | 567 | +15.3% | 📅 |
| **Активные туры** | 89 | +5 | 🏔️ |

**Пример карточки:**
```tsx
<div className="bg-white/15 border border-white/15 rounded-2xl p-6">
  <div className="flex items-center justify-between mb-2">
    <span className="text-white/70 text-sm">Выручка</span>
    <span className="text-2xl">💰</span>
  </div>
  <div className="text-3xl font-bold text-white mb-1">
    1 234 567 ₽
  </div>
  <div className="text-sm text-green-400">
    ↗ +12.5% за период
  </div>
</div>
```

---

### 4. Графики (Charts Row)

**2 графика рядом:**

#### 📈 График 1: Выручка по месяцам
```tsx
<SimpleChart
  title="Выручка по месяцам"
  data={data.charts.revenueByMonth.map(point => ({
    label: point.date,
    value: point.value
  }))}
  type="line"
  valueFormatter={formatCurrency}
/>
```
**Тип:** Линейный график  
**Данные:** Массив точек `{ date: 'Янв', value: 123456 }`

---

#### 📊 График 2: Бронирования по категориям
```tsx
<SimpleChart
  title="Бронирования по категориям"
  data={data.charts.bookingsByCategory.map(cat => ({
    label: cat.category,
    value: cat.value,
    color: cat.color
  }))}
  type="bar"
  valueFormatter={(v) => v.toString()}
/>
```
**Тип:** Столбчатая диаграмма  
**Категории:**
- Туры
- Размещение
- Трансферы
- Снаряжение

---

### 5. Топ туры (Top Tours)

```tsx
{data.charts.topTours.length > 0 && (
  <div className="bg-white/15 border border-white/15 rounded-2xl p-6">
    <h3 className="text-lg font-bold text-white mb-4">Топ туры</h3>
    <div className="space-y-3">
      {data.charts.topTours.map((tour, index) => (
        <div className="flex items-center justify-between p-4 bg-white/15 rounded-xl">
          <div className="flex items-center space-x-4">
            <div className="w-8 h-8 rounded-full bg-premium-gold text-premium-black font-bold">
              {index + 1}
            </div>
            <div>
              <p className="font-semibold text-white">{tour.title}</p>
              <p className="text-sm text-white/60">{tour.bookings} бронирований</p>
            </div>
          </div>
          <div className="text-right">
            <p className="text-lg font-bold text-white">{formatCurrency(tour.revenue)}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
)}
```

**Пример топ тура:**
```
🥇 1. Восхождение на Авачинский вулкан
      45 бронирований | 567 890 ₽
```

---

### 6. Последние действия (Recent Activities)

```tsx
<RecentActivities activities={data.recentActivities} />
```

**Типы действий:**
- 👤 Новый пользователь зарегистрировался
- 📅 Новое бронирование создано
- ⭐ Новый отзыв оставлен
- 🏔️ Новый тур опубликован
- 💰 Платёж получен

**Пример:**
```
👤 Иван Петров зарегистрировался
   2 минуты назад

📅 Новое бронирование: "Восхождение на вулкан"
   15 минут назад

⭐ Новый отзыв на тур "Рыбалка на Камчатке"
   1 час назад
```

---

### 7. Быстрые действия (Quick Actions)

```tsx
<div className="bg-white/15 border border-white/15 rounded-2xl p-6">
  <h3 className="text-lg font-bold text-white mb-4">Быстрые действия</h3>
  <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
    <button className="p-4 bg-white/10 hover:bg-white/20 rounded-xl text-center">
      <div className="text-3xl mb-2">👥</div>
      <p className="text-sm font-semibold">Пользователи</p>
    </button>
    <button className="p-4 bg-white/10 hover:bg-white/20 rounded-xl text-center">
      <div className="text-3xl mb-2">🏔️</div>
      <p className="text-sm font-semibold">Туры</p>
    </button>
    <button className="p-4 bg-white/10 hover:bg-white/20 rounded-xl text-center">
      <div className="text-3xl mb-2">💳</div>
      <p className="text-sm font-semibold">Транзакции</p>
    </button>
    <button className="p-4 bg-white/10 hover:bg-white/20 rounded-xl text-center">
      <div className="text-3xl mb-2">⚙️</div>
      <p className="text-sm font-semibold">Настройки</p>
    </button>
  </div>
</div>
```

---

## 📡 API для Dashboard

### Endpoint
```
GET /api/admin/dashboard?period={period}
```

### Query параметры
- `period`: `7` | `30` | `90` | `365` (дней)

### Ответ
```json
{
  "success": true,
  "data": {
    "metrics": {
      "totalRevenue": 1234567,
      "revenueChange": 12.5,
      "totalUsers": 1234,
      "usersChange": 8.2,
      "totalBookings": 567,
      "bookingsChange": 15.3,
      "activeTours": 89,
      "toursChange": 5
    },
    "charts": {
      "revenueByMonth": [
        { "date": "Янв", "value": 123456 },
        { "date": "Фев", "value": 234567 }
      ],
      "bookingsByCategory": [
        { "category": "Туры", "value": 45, "color": "#3b82f6" },
        { "category": "Размещение", "value": 23, "color": "#10b981" }
      ],
      "topTours": [
        {
          "id": "tour-1",
          "title": "Восхождение на Авачинский вулкан",
          "bookings": 45,
          "revenue": 567890
        }
      ]
    },
    "recentActivities": [
      {
        "id": "1",
        "type": "user_registered",
        "message": "Иван Петров зарегистрировался",
        "timestamp": "2025-11-27T12:34:56Z"
      }
    ]
  }
}
```

---

## 🔄 Логика загрузки данных

```tsx
const fetchDashboardData = async () => {
  try {
    setLoading(true);
    setError(null);
    
    const response = await fetch(`/api/admin/dashboard?period=${period}`);
    const result = await response.json();

    if (!result.success) {
      throw new Error(result.error || 'Failed to fetch data');
    }

    setData(result.data);
  } catch (err) {
    console.error('Error fetching dashboard data:', err);
    setError(err instanceof Error ? err.message : 'Unknown error');
  } finally {
    setLoading(false);
  }
};
```

---

## 🎯 Состояния страницы

### 1. Загрузка (Loading)
```tsx
{loading && (
  <div className="flex items-center justify-center py-20">
    <LoadingSpinner size="lg" message="Загрузка данных..." />
  </div>
)}
```

---

### 2. Ошибка (Error)
```tsx
{error && (
  <div className="bg-red-500/20 border border-red-500/50 rounded-2xl p-6 text-center">
    <div className="text-4xl mb-2">⚠️</div>
    <h3 className="text-lg font-bold text-red-400 mb-2">Ошибка загрузки</h3>
    <p className="text-white/70">{error}</p>
    <button onClick={fetchDashboardData}>Попробовать снова</button>
  </div>
)}
```

---

### 3. Данные загружены (Success)
```tsx
{!loading && !error && data && (
  <div className="space-y-6">
    <MetricsGrid metrics={data.metrics} />
    {/* ... остальные компоненты ... */}
  </div>
)}
```

---

---

# 3. УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ

## 📍 Информация

**URL:** `/hub/admin/users`  
**Файл:** `app/hub/admin/users/page.tsx`  
**API:** `/api/admin/users`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Управление всеми пользователями платформы:**
- Просмотр списка пользователей
- Поиск по имени, email
- Фильтрация по роли
- Просмотр детальной информации
- Блокировка/разблокировка пользователей
- Изменение роли

---

## 📊 Структура страницы

### 1. Заголовок
```tsx
<div className="bg-white/15 border-b border-white/15 p-6">
  <h1 className="text-3xl font-black text-white">Управление пользователями</h1>
  <p className="text-white/70 mt-1">Все зарегистрированные пользователи платформы</p>
</div>
```

---

### 2. Инструменты поиска и фильтрации

**Поисковая строка:**
```tsx
<SearchBar 
  placeholder="Поиск по имени, email..."
  onSearch={handleSearch}
/>
```

**Фильтр по роли:**
```tsx
<select value={roleFilter} onChange={(e) => setRoleFilter(e.target.value)}>
  <option value="">Все роли</option>
  <option value="tourist">Турист</option>
  <option value="operator">Оператор</option>
  <option value="guide">Гид</option>
  <option value="transfer">Трансфер</option>
  <option value="agent">Агент</option>
  <option value="admin">Админ</option>
</select>
```

---

### 3. Таблица пользователей

**Колонки:**

| Колонка | Описание | Сортировка | Render |
|---------|----------|------------|--------|
| **Пользователь** | Имя + Email | ✅ | `name` + `email` (серый) |
| **Роль** | Роль пользователя | ✅ | Badge (золотой фон) |
| **Статус** | Active/Inactive | ❌ | `<StatusBadge>` |
| **Бронирования** | Количество | ✅ | Число |
| **Выручка** | Потрачено | ✅ | `formatCurrency` |
| **Регистрация** | Дата | ✅ | `formatDate` |
| **Действия** | Кнопки | ❌ | Блокировать / Просмотр |

---

**Пример строки:**
```tsx
<tr>
  <td>
    <div>
      <p className="font-semibold text-white">Иван Петров</p>
      <p className="text-xs text-white/60">ivan@example.com</p>
    </div>
  </td>
  <td>
    <span className="px-3 py-1 bg-premium-gold/20 text-white rounded-full text-xs font-bold">
      Турист
    </span>
  </td>
  <td><StatusBadge status="active" /></td>
  <td>12</td>
  <td>45 678 ₽</td>
  <td>12 Ноя 2025</td>
  <td>
    <button>Блокировать</button>
    <button>Просмотр</button>
  </td>
</tr>
```

---

### 4. Пагинация

```tsx
<Pagination 
  currentPage={currentPage} 
  totalPages={totalPages} 
  onPageChange={setCurrentPage}
/>
```

**Показывает:** 20 пользователей на странице

---

## 📡 API

### Получить список пользователей
```
GET /api/admin/users?page=1&limit=20&search=ivan&role=tourist
```

**Query параметры:**
- `page` — номер страницы (по умолчанию: 1)
- `limit` — количество на странице (по умолчанию: 20)
- `search` — поиск по имени/email
- `role` — фильтр по роли

**Ответ:**
```json
{
  "success": true,
  "data": {
    "data": [
      {
        "id": "user-1",
        "name": "Иван Петров",
        "email": "ivan@example.com",
        "role": "tourist",
        "status": "active",
        "bookingsCount": 12,
        "totalSpent": 45678,
        "createdAt": "2025-11-12T10:00:00Z"
      }
    ],
    "pagination": {
      "currentPage": 1,
      "totalPages": 5,
      "totalItems": 98,
      "itemsPerPage": 20
    }
  }
}
```

---

## ⚡ Действия администратора

### 1️⃣ Блокировать пользователя

**API:**
```
POST /api/admin/users/[id]/block
```

**Что делает:**
- Устанавливает `status: 'blocked'`
- Пользователь не может войти
- Уведомление пользователю

---

### 2️⃣ Разблокировать пользователя

**API:**
```
POST /api/admin/users/[id]/unblock
```

---

### 3️⃣ Изменить роль

**API:**
```
PATCH /api/admin/users/[id]
Body: { "role": "operator" }
```

---

### 4️⃣ Просмотреть детали

**Переход на:** `/hub/admin/users/[id]` (если страница существует)

**Или модальное окно с данными:**
- Полная информация о пользователе
- История бронирований
- История платежей
- История отзывов
- Логи активности

---

## 🔍 Утилиты

### Форматирование роли
```tsx
const getRoleLabel = (role: string) => {
  const labels: Record<string, string> = {
    tourist: 'Турист',
    operator: 'Оператор',
    guide: 'Гид',
    transfer: 'Трансфер',
    agent: 'Агент',
    admin: 'Админ'
  };
  return labels[role] || role;
};
```

---

### Форматирование валюты
```tsx
const formatCurrency = (value: number) => {
  return new Intl.NumberFormat('ru-RU', {
    style: 'currency',
    currency: 'RUB',
    minimumFractionDigits: 0
  }).format(value);
};
```

---

### Форматирование даты
```tsx
const formatDate = (date: Date) => {
  return new Intl.DateTimeFormat('ru-RU', {
    year: 'numeric',
    month: 'short',
    day: 'numeric'
  }).format(new Date(date));
};
```

---

---

# 4. МОДЕРАЦИЯ ТУРОВ

## 📍 Информация

**URL:** `/hub/admin/content/tours`  
**Файл:** `app/hub/admin/content/tours/page.tsx`  
**API:** `/api/admin/content/tours`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Модерация туров, созданных туроператорами:**
- Одобрение новых туров
- Деактивация проблемных туров
- Архивирование нарушающих правила
- Просмотр детальной информации

---

## 📊 Таблица туров

**Колонки:**

| Колонка | Описание | Сортировка |
|---------|----------|------------|
| **Название** | Название + превью | ✅ |
| **Оператор** | Кто создал | ✅ |
| **Сложность** | Легко/Средне/Сложно | ❌ |
| **Длительность** | Часы/Дни | ✅ |
| **Цена** | Стоимость | ✅ |
| **Рейтинг** | ⭐ 0-5 (отзывы) | ✅ |
| **Статус** | Активный/Неактивный | ❌ |
| **Действия** | Toggle / Архив / Просмотр | ❌ |

---

## ⚡ Действия

### 1️⃣ Активировать/Деактивировать
```tsx
const handleToggleActive = async (tourId: string, currentStatus: boolean) => {
  const response = await fetch(`/api/admin/content/tours/${tourId}`, {
    method: 'PUT',
    body: JSON.stringify({ isActive: !currentStatus })
  });
  if (response.ok) fetchTours();
};
```

**API:**
```
PUT /api/admin/content/tours/[id]
Body: { "isActive": true }
```

---

### 2️⃣ Архивировать
```tsx
const handleArchive = async (tourId: string) => {
  if (!confirm('Вы уверены, что хотите архивировать этот тур?')) return;
  
  const response = await fetch(`/api/admin/content/tours/${tourId}`, {
    method: 'DELETE'
  });
  if (response.ok) fetchTours();
};
```

**API:**
```
DELETE /api/admin/content/tours/[id]
```

---

## 🔍 Инструменты

### Поиск
**Ищет по:**
- Название тура
- ID тура
- Имя оператора

### Фильтры
- Все туры
- Активные
- Неактивные

---

**Подробнее см. документ:** `📝_КАК_АДМИНИСТРАТОР_МОДЕРИРУЕТ.md`

---

---

# 5. МОДЕРАЦИЯ ПАРТНЁРОВ

## 📍 Информация

**URL:** `/hub/admin/content/partners`  
**Файл:** `app/hub/admin/content/partners/page.tsx`  
**API:** `/api/admin/content/partners`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Модерация партнёров (бизнес-аккаунтов):**
- Верификация новых партнёров
- Проверка документов
- Отклонение фейковых регистраций
- Блокировка нарушителей

---

## 📊 Таблица партнёров

**Колонки:**

| Колонка | Описание |
|---------|----------|
| **Название** | Логотип + название + категория |
| **Контакт** | Email/телефон |
| **Рейтинг** | ⭐ 0-5 (отзывы) |
| **Верификация** | ✅ Верифицирован / ⏳ На проверке |
| **Действия** | Верифицировать / Просмотр / Редактировать |

---

## ⚡ Действия

### Верифицировать партнёра
```tsx
const handleVerify = async (partnerId: string) => {
  const response = await fetch(`/api/admin/content/partners/${partnerId}/verify`, {
    method: 'POST'
  });
  if (response.ok) fetchPartners();
};
```

**API:**
```
POST /api/admin/content/partners/[id]/verify
```

---

## 🔍 Фильтры

### По категории:
- 🏨 Размещение
- 🏢 Туроператор
- 🚗 Трансфер
- 🎒 Гид
- 🎁 Сувениры
- ⛏️ Снаряжение
- 🍽️ Ресторан

### По верификации:
- Все
- Верифицированные
- На проверке
- Отклонённые

---

**Подробнее см. документ:** `📝_КАК_АДМИНИСТРАТОР_МОДЕРИРУЕТ.md`

---

---

# 6. МОДЕРАЦИЯ ОТЗЫВОВ

## 📍 Информация

**URL:** `/hub/admin/content/reviews`  
**Файл:** `app/hub/admin/content/reviews/page.tsx`  
**API:** `/api/admin/content/reviews`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Модерация отзывов туристов:**
- Одобрение настоящих отзывов
- Удаление спама, мата, фейков
- Редактирование отзывов
- Проверка реальности отзывов

---

## 📊 Таблица отзывов

**Колонки:**

| Колонка | Описание |
|---------|----------|
| **Пользователь** | Имя туриста |
| **Тур** | Название тура |
| **Оценка** | ⭐⭐⭐⭐⭐ (1-5) |
| **Комментарий** | Текст отзыва (truncate) |
| **Дата** | Когда написан |
| **Статус** | ✅ Одобрен / ⏳ На проверке / ❌ Отклонён |
| **Действия** | Одобрить / Удалить / Просмотр |

---

## ⚡ Действия

### Одобрить отзыв
```tsx
const handleModerate = async (reviewId: string, action: 'approve' | 'delete') => {
  const response = await fetch(`/api/admin/content/reviews/${reviewId}/moderate`, {
    method: 'POST',
    body: JSON.stringify({ action })
  });
  if (response.ok) fetchReviews();
};
```

**API:**
```
POST /api/admin/content/reviews/[id]/moderate
Body: { "action": "approve" | "delete" }
```

---

## 🔍 Фильтры

- Все отзывы
- Одобренные
- На модерации
- Отклонённые

**По оценке (опционально):**
- ⭐⭐⭐⭐⭐ 5 звёзд
- ⭐⭐⭐⭐ 4 звезды
- ⭐⭐⭐ 3 звезды
- ⭐⭐ 2 звезды
- ⭐ 1 звезда

---

**Подробнее см. документ:** `📝_КАК_АДМИНИСТРАТОР_МОДЕРИРУЕТ.md`

---

---

# 7. ФИНАНСОВОЕ УПРАВЛЕНИЕ

## 📍 Информация

**URL:** `/hub/admin/finance`  
**Файл:** `app/hub/admin/finance/page.tsx`  
**API:** `/api/admin/finance/*`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Финансовое управление платформой:**
- Мониторинг доходов
- Управление выплатами партнёрам
- Финансовая аналитика
- Отчёты

---

## 📑 Вкладки (Tabs)

### 1️⃣ Обзор (Overview)
### 2️⃣ Выплаты (Payouts)

**Переключение:**
```tsx
const tabs = [
  { id: 'overview', name: 'Обзор', icon: '📊' },
  { id: 'payouts', name: 'Выплаты', icon: '💸' },
];
```

---

## 📊 ВКЛАДКА 1: ОБЗОР (OVERVIEW)

### Селектор периода
```tsx
<select value={period} onChange={(e) => setPeriod(e.target.value)}>
  <option value="7">7 дней</option>
  <option value="30">30 дней</option>
  <option value="90">90 дней</option>
  <option value="365">Год</option>
</select>
```

---

### Сетка метрик (FinanceMetricsGrid)

**Метрики:**

| Метрика | Описание | Иконка |
|---------|----------|--------|
| **Общая выручка** | Всего заработано | 💰 |
| **Комиссия платформы** | Доход платформы | 💵 |
| **Выплачено партнёрам** | Сумма выплат | 💸 |
| **Ожидает выплаты** | В обработке | ⏳ |

**Компонент:**
```tsx
<FinanceMetricsGrid period={period} />
```

---

### График выручки (RevenueChart)

**График доходов по времени:**
```tsx
<RevenueChart period={period} />
```

**Данные:**
- Выручка по дням/месяцам
- Комиссия платформы
- Выплаты партнёрам

---

## 💸 ВКЛАДКА 2: ВЫПЛАТЫ (PAYOUTS)

### Компонент PayoutsManager

```tsx
<PayoutsManager />
```

**Функции:**
- Просмотр всех запросов на выплату
- Одобрение выплат
- Отклонение выплат
- История выплат

---

### Таблица выплат

**Колонки:**

| Колонка | Описание |
|---------|----------|
| **Партнёр** | Название компании |
| **Сумма** | Сумма выплаты |
| **Дата запроса** | Когда создан запрос |
| **Статус** | Ожидает / Одобрено / Выплачено / Отклонено |
| **Действия** | Одобрить / Отклонить / Просмотр |

---

### Действия

#### Одобрить выплату
```
POST /api/admin/finance/payouts/[id]/approve
```

#### Отклонить выплату
```
POST /api/admin/finance/payouts/[id]/reject
Body: { "reason": "Недостаточно данных" }
```

#### Пометить как выплачено
```
POST /api/admin/finance/payouts/[id]/complete
Body: { "transactionId": "TXN123456" }
```

---

## 📡 API Endpoints

```
GET  /api/admin/finance/metrics?period=30
GET  /api/admin/finance/revenue?period=30
GET  /api/admin/finance/payouts?status=pending
POST /api/admin/finance/payouts/[id]/approve
POST /api/admin/finance/payouts/[id]/reject
POST /api/admin/finance/payouts/[id]/complete
```

---

---

# 8. НАСТРОЙКИ СИСТЕМЫ

## 📍 Информация

**URL:** `/hub/admin/settings`  
**Файл:** `app/hub/admin/settings/page.tsx`  
**API:** `/api/admin/settings/*`  
**Защита:** `<Protected roles={['admin']}>`

---

## 🎯 Назначение

**Конфигурация платформы:**
- Системные настройки
- Email шаблоны
- Интеграции
- Параметры безопасности

---

## 📑 Вкладки (Tabs)

### 1️⃣ Системные настройки (System)
### 2️⃣ Email шаблоны (Email)

**Переключение:**
```tsx
const tabs = [
  { id: 'system', name: 'Системные настройки', icon: '⚙️' },
  { id: 'email', name: 'Email шаблоны', icon: '📧' },
];
```

---

## ⚙️ ВКЛАДКА 1: СИСТЕМНЫЕ НАСТРОЙКИ

### Компонент SystemSettings

```tsx
<SystemSettings />
```

**Группы настроек:**

### 1. Общие настройки
- **Название платформы** (текст)
- **Email поддержки** (email)
- **Телефон поддержки** (tel)
- **Язык по умолчанию** (select)
- **Часовой пояс** (select)

---

### 2. Финансовые настройки
- **Комиссия платформы** (%)
- **Минимальная сумма выплаты** (₽)
- **Валюта по умолчанию** (RUB/USD/EUR)
- **Налог (НДС)** (%)

---

### 3. Безопасность
- **Двухфакторная аутентификация** (checkbox)
- **Максимальное время сессии** (минуты)
- **Попытки входа до блокировки** (число)
- **IP whitelist** (textarea)

---

### 4. Интеграции
- **Google Maps API Key** (текст)
- **Яндекс.Касса Shop ID** (текст)
- **Яндекс.Касса Secret Key** (password)
- **Telegram Bot Token** (password)
- **Email SMTP Host** (текст)
- **Email SMTP Port** (число)
- **Email SMTP User** (текст)
- **Email SMTP Password** (password)

---

### 5. Лимиты
- **Максимальный размер файла** (МБ)
- **Максимум изображений в галерее** (число)
- **Максимум туров на оператора** (число)
- **Максимум бронирований в день** (число)

---

**Кнопка сохранения:**
```tsx
<button onClick={handleSaveSettings}>
  💾 Сохранить настройки
</button>
```

**API:**
```
PUT /api/admin/settings
Body: { "settings": { ... } }
```

---

## 📧 ВКЛАДКА 2: EMAIL ШАБЛОНЫ

### Компонент EmailTemplatesManager

```tsx
<EmailTemplatesManager />
```

**Список шаблонов:**

| Шаблон | Событие | Переменные |
|--------|---------|------------|
| **Регистрация** | Новый пользователь | `{name}`, `{email}`, `{link}` |
| **Подтверждение email** | Email верификация | `{name}`, `{code}` |
| **Новое бронирование** | Турист забронировал | `{name}`, `{tour}`, `{date}`, `{price}` |
| **Подтверждение бронирования** | Оператор подтвердил | `{name}`, `{tour}`, `{date}` |
| **Отмена бронирования** | Бронирование отменено | `{name}`, `{tour}`, `{reason}` |
| **Новый отзыв** | Турист оставил отзыв | `{name}`, `{tour}`, `{rating}` |
| **Запрос на выплату** | Партнёр запросил выплату | `{name}`, `{amount}` |
| **Выплата одобрена** | Админ одобрил выплату | `{name}`, `{amount}`, `{date}` |
| **Выплата отклонена** | Админ отклонил выплату | `{name}`, `{reason}` |
| **Верификация партнёра** | Партнёр верифицирован | `{name}` |

---

### Редактор шаблона

**Поля:**
- **Тема письма** (текст)
- **Тело письма** (HTML редактор / Markdown)
- **Переменные** (подсказка доступных)
- **Превью** (как будет выглядеть)

**Пример:**
```html
<h1>Здравствуйте, {name}!</h1>
<p>Ваше бронирование тура "{tour}" подтверждено.</p>
<p>Дата: {date}</p>
<p>Стоимость: {price}</p>
<a href="{link}">Посмотреть детали</a>
```

**Кнопки:**
- 💾 Сохранить шаблон
- 📧 Отправить тестовый email
- ↺ Сбросить к умолчанию

---

**API:**
```
GET  /api/admin/settings/email-templates
PUT  /api/admin/settings/email-templates/[id]
POST /api/admin/settings/email-templates/[id]/test
```

---

---

# 9. НАВИГАЦИЯ И ЗАЩИТА

## 🧭 Компонент AdminNav

**Файл:** `components/admin/AdminNav.tsx`

### Структура навигации

```tsx
const navItems: NavItem[] = [
  { name: 'Dashboard', path: '/hub/admin', icon: '📊' },
  { name: 'Пользователи', path: '/hub/admin/users', icon: '👥' },
  { name: 'Туры', path: '/hub/admin/content/tours', icon: '🏔️' },
  { name: 'Партнёры', path: '/hub/admin/content/partners', icon: '🤝' },
  { name: 'Отзывы', path: '/hub/admin/content/reviews', icon: '💬' },
  { name: 'Финансы', path: '/hub/admin/finance', icon: '💰' },
  { name: 'Настройки', path: '/hub/admin/settings', icon: '⚙️' },
];
```

---

### UI навигации

```tsx
<nav className="bg-white/5 border-b border-white/10 sticky top-0 z-10">
  <div className="max-w-7xl mx-auto px-6">
    <div className="flex items-center justify-between h-16">
      {/* Логотип */}
      <Link href="/hub/admin" className="text-2xl font-black text-premium-gold">
        Admin Panel
      </Link>

      {/* Навигация */}
      <div className="flex items-center space-x-2">
        {navItems.map((item) => {
          const isActive = pathname === item.path;
          
          return (
            <Link
              key={item.path}
              href={item.path}
              className={clsx(
                'px-4 py-2 rounded-lg font-medium transition-all duration-200',
                isActive
                  ? 'bg-premium-gold text-premium-black'
                  : 'text-white/70 hover:bg-white/10 hover:text-white'
              )}
            >
              <span className="mr-2">{item.icon}</span>
              {item.name}
            </Link>
          );
        })}
      </div>

      {/* Меню пользователя */}
      <div className="flex items-center">
        <button className="p-2 rounded-lg hover:bg-white/10 transition-colors">
          <span className="text-2xl">👤</span>
        </button>
      </div>
    </div>
  </div>
</nav>
```

---

### Активная страница

**Подсветка активной кнопки:**
```tsx
const isActive = pathname === item.path;

className={clsx(
  'px-4 py-2 rounded-lg font-medium',
  isActive
    ? 'bg-premium-gold text-premium-black'
    : 'text-white/70 hover:bg-white/10 hover:text-white'
)}
```

---

## 🔒 Защита страниц (Protected Component)

**Файл:** `components/Protected.tsx`

### Как работает защита

```tsx
<Protected roles={['admin']}>
  <main>
    {/* Контент страницы администратора */}
  </main>
</Protected>
```

---

### Логика защиты

```tsx
export const Protected: React.FC<ProtectedProps> = ({ 
  children, 
  roles, 
  fallback = <div className="text-center p-8 text-gray-500">Нет доступа</div> 
}) => {
  const { hasRole } = useRoles();
  
  const hasAccess = roles.some(role => hasRole(role as any));
  
  if (!hasAccess) {
    return <>{fallback}</>;
  }
  
  return <>{children}</>;
};
```

---

### Контекст ролей (RoleContext)

**Файл:** `contexts/RoleContext.tsx`

```tsx
const { hasRole, roles } = useRoles();

// Проверка роли
if (hasRole('admin')) {
  // Доступ разрешён
}

// Множественные роли
if (hasRole('admin') || hasRole('operator')) {
  // Доступ разрешён
}
```

---

### Fallback при отсутствии доступа

**По умолчанию:**
```tsx
<div className="text-center p-8 text-gray-500">
  Нет доступа
</div>
```

**Кастомный fallback:**
```tsx
<Protected 
  roles={['admin']} 
  fallback={
    <div className="min-h-screen flex items-center justify-center">
      <div className="bg-red-500/20 border border-red-500/50 rounded-2xl p-8 text-center">
        <div className="text-6xl mb-4">🚫</div>
        <h2 className="text-2xl font-bold text-white mb-2">Доступ запрещён</h2>
        <p className="text-white/70">У вас нет прав для просмотра этой страницы.</p>
        <button onClick={() => router.push('/')}>Вернуться на главную</button>
      </div>
    </div>
  }
>
  {/* Защищённый контент */}
</Protected>
```

---

---

# 📊 СВОДНАЯ ТАБЛИЦА ВСЕХ СТРАНИЦ

| № | Название | URL | Файл | Защита | API | Компоненты |
|---|----------|-----|------|--------|-----|------------|
| **0** | **Вход** | `/auth/login` | `app/auth/login/page.tsx` | ❌ Нет | `/api/auth/login` | - |
| **1** | **Dashboard** | `/hub/admin` | `app/hub/admin/page.tsx` | ✅ Admin | `/api/admin/dashboard` | `MetricsGrid`, `SimpleChart`, `RecentActivities` |
| **2** | **Пользователи** | `/hub/admin/users` | `app/hub/admin/users/page.tsx` | ✅ Admin | `/api/admin/users` | `DataTable`, `SearchBar`, `Pagination` |
| **3** | **Туры** | `/hub/admin/content/tours` | `app/hub/admin/content/tours/page.tsx` | ✅ Admin | `/api/admin/content/tours` | `DataTable`, `SearchBar`, `Pagination` |
| **4** | **Партнёры** | `/hub/admin/content/partners` | `app/hub/admin/content/partners/page.tsx` | ✅ Admin | `/api/admin/content/partners` | `DataTable`, `SearchBar`, `Pagination` |
| **5** | **Отзывы** | `/hub/admin/content/reviews` | `app/hub/admin/content/reviews/page.tsx` | ✅ Admin | `/api/admin/content/reviews` | `DataTable`, `Pagination` |
| **6** | **Финансы** | `/hub/admin/finance` | `app/hub/admin/finance/page.tsx` | ✅ Admin | `/api/admin/finance/*` | `FinanceMetricsGrid`, `RevenueChart`, `PayoutsManager` |
| **7** | **Настройки** | `/hub/admin/settings` | `app/hub/admin/settings/page.tsx` | ✅ Admin | `/api/admin/settings/*` | `SystemSettings`, `EmailTemplatesManager` |

---

---

# 🔄 ПУТЬ ПОЛЬЗОВАТЕЛЯ: АДМИНИСТРАТОР

## Сценарий 1: Вход и обзор

```
1. Администратор → http://147.45.158.166/auth/login
2. Вводит Email + Пароль → Нажимает "Войти"
3. Перенаправление → http://147.45.158.166/hub/admin
4. Видит Dashboard:
   - Метрики (выручка, пользователи, бронирования, туры)
   - Графики (выручка, бронирования)
   - Топ туры
   - Последние действия
5. Кликает "Обновить" → Данные обновляются
6. Меняет период на "7 дней" → Графики пересчитываются
```

---

## Сценарий 2: Модерация нового тура

```
1. Dashboard → Видит уведомление: "Новый тур ожидает модерации"
2. Кликает на навигации "Туры" → /hub/admin/content/tours
3. Видит таблицу туров, фильтр "Неактивные" → 1 новый тур
4. Тур: "Восхождение на Авачинский вулкан" (статус: неактивный)
5. Кликает "Просмотр" 👁️ → Видит детали:
   - Описание
   - Маршрут
   - Цены
   - Изображения
   - Оператор (верифицирован)
6. Всё корректно → Кликает переключатель "Активировать"
7. API: PUT /api/admin/content/tours/tour-123 { isActive: true }
8. Тур становится активным → Появляется в каталоге /tours
9. Оператор получает email: "Ваш тур одобрен!"
```

---

## Сценарий 3: Верификация партнёра

```
1. Dashboard → "Новый партнёр ожидает верификации"
2. Навигация → "Партнёры" → /hub/admin/content/partners
3. Фильтр "На проверке" → Партнёр: "Камчатские Приключения ООО"
4. Кликает "Просмотр" → Видит:
   - ИНН: 4101234567
   - ОГРН: 1234567890123
   - Адрес: г. Петропавловск-Камчатский
   - Контакты: +7 (999) 123-45-67
   - Документы (скан лицензии)
5. Проверяет ИНН в базе ФНС → Компания существует
6. Звонит по телефону → Подтверждает контакт
7. Кликает "Верифицировать" ✅
8. API: POST /api/admin/content/partners/partner-456/verify
9. Партнёр получает бейдж "Проверено" 🏆
10. Email партнёру: "Поздравляем! Вы верифицированы"
```

---

## Сценарий 4: Модерация отзыва

```
1. Dashboard → "Новый отзыв ожидает модерации"
2. Навигация → "Отзывы" → /hub/admin/content/reviews
3. Фильтр "На модерации" → Отзыв:
   - Турист: Иван Петров
   - Тур: "Рыбалка на Камчатке"
   - Оценка: ⭐⭐⭐⭐⭐
   - Текст: "Отличный тур! Гид очень профессиональный..."
4. Кликает "Просмотр" → Видит полный текст
5. Проверяет:
   - Турист реально бронировал этот тур? ✅ Да (проверка в базе)
   - Нет мата? ✅ Нет
   - Нет спама? ✅ Нет
   - Конструктивный? ✅ Да
6. Кликает "Одобрить" ✅
7. API: POST /api/admin/content/reviews/review-789/moderate { action: "approve" }
8. Отзыв опубликован → Рейтинг тура обновлён (4.7 → 4.8)
9. Email туристу: "Ваш отзыв опубликован!"
```

---

## Сценарий 5: Одобрение выплаты

```
1. Навигация → "Финансы" → /hub/admin/finance
2. Вкладка "Выплаты" → Видит запрос:
   - Партнёр: "Камчатские Приключения ООО"
   - Сумма: 125 000 ₽
   - Дата запроса: 25 Ноября 2025
   - Статус: Ожидает одобрения
3. Кликает "Просмотр" → Видит:
   - Детали выплаты
   - Банковские реквизиты
   - История бронирований (подтверждение выручки)
4. Всё корректно → Кликает "Одобрить" ✅
5. API: POST /api/admin/finance/payouts/payout-999/approve
6. Статус: "Одобрено" → Админ выполняет перевод вручную
7. После перевода → Кликает "Пометить как выплачено"
8. API: POST /api/admin/finance/payouts/payout-999/complete { transactionId: "TXN123456" }
9. Статус: "Выплачено"
10. Email партнёру: "Выплата выполнена: 125 000 ₽"
```

---

## Сценарий 6: Изменение настроек

```
1. Навигация → "Настройки" → /hub/admin/settings
2. Вкладка "Системные настройки"
3. Изменяет:
   - Комиссия платформы: 10% → 12%
   - Минимальная сумма выплаты: 5 000 ₽ → 10 000 ₽
4. Кликает "Сохранить настройки" 💾
5. API: PUT /api/admin/settings { commission: 12, minPayout: 10000 }
6. Настройки сохранены → Уведомление: "Настройки успешно обновлены"
7. Изменения вступают в силу немедленно
```

---

---

# 🎨 ДИЗАЙН СИСТЕМЫ (SAMSUNG WEATHER ТЕМА)

## Общие стили

**Все страницы администратора используют Samsung Weather тему:**

### Цвета
```css
--premium-gold: #FFD700;
--premium-black: #0A0E27;
--text-white: #FFFFFF;
--text-white-70: rgba(255, 255, 255, 0.7);
--bg-white-15: rgba(255, 255, 255, 0.15);
--bg-white-10: rgba(255, 255, 255, 0.1);
--bg-white-5: rgba(255, 255, 255, 0.05);
```

---

### Карточки (Cards)
```tsx
className="bg-white/15 border border-white/15 rounded-2xl p-6 
           backdrop-blur-2xl shadow-2xl"
```

---

### Кнопки (Buttons)

**Первичная кнопка:**
```tsx
className="px-6 py-3 bg-gradient-to-r from-sky-200 to-cyan-200 
           text-premium-black font-bold rounded-xl 
           hover:shadow-lg hover:shadow-blue-500/50 
           transition-all transform hover:scale-105"
```

**Вторичная кнопка:**
```tsx
className="px-6 py-3 bg-white/15 border border-white/15 
           text-white rounded-xl hover:bg-white/10 
           transition-all"
```

**Кнопка в навигации (активная):**
```tsx
className="px-4 py-2 bg-premium-gold text-premium-black 
           rounded-lg font-medium"
```

**Кнопка в навигации (неактивная):**
```tsx
className="px-4 py-2 text-white/70 hover:bg-white/10 
           hover:text-white rounded-lg font-medium 
           transition-all duration-200"
```

---

### Поля ввода (Inputs)
```tsx
className="w-full px-6 py-4 bg-white/15 border border-white/15 
           rounded-xl focus:outline-none focus:ring-2 focus:ring-sky-300 
           focus:border-transparent text-white placeholder-white/40 
           transition-all"
```

---

### Таблицы (Tables)
```tsx
className="w-full border-collapse"

// Заголовок
className="bg-white/15 border-b border-white/15 text-left text-white/70 
           text-sm font-semibold uppercase tracking-wide"

// Строка
className="border-b border-white/15 hover:bg-white/10 transition-colors"

// Ячейка
className="p-4 text-white"
```

---

### Badge (Статусы)
```tsx
// Активный
className="px-3 py-1 bg-green-500/20 text-green-400 
           rounded-full text-xs font-bold"

// Неактивный
className="px-3 py-1 bg-red-500/20 text-red-400 
           rounded-full text-xs font-bold"

// На проверке
className="px-3 py-1 bg-yellow-500/20 text-yellow-400 
           rounded-full text-xs font-bold"
```

---

### Анимации
```css
/* Pulse */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* Bounce */
@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-25px); }
}

/* Shake */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10px); }
  75% { transform: translateX(10px); }
}
```

---

---

# 📊 СТАТИСТИКА ПРОЕКТА

## Файлы администратора

| Категория | Количество |
|-----------|------------|
| **Страницы** | 7 |
| **Компоненты** | 16 |
| **API endpoints** | 15 |
| **Типы TypeScript** | 5 |

---

## Структура файлов

```
app/
├── auth/
│   └── login/
│       └── page.tsx ..................... Страница входа
├── hub/
│   └── admin/
│       ├── page.tsx .................... Dashboard
│       ├── users/
│       │   └── page.tsx ................ Управление пользователями
│       ├── content/
│       │   ├── tours/
│       │   │   └── page.tsx ............ Модерация туров
│       │   ├── partners/
│       │   │   └── page.tsx ............ Модерация партнёров
│       │   └── reviews/
│       │       └── page.tsx ............ Модерация отзывов
│       ├── finance/
│       │   └── page.tsx ................ Финансовое управление
│       └── settings/
│           └── page.tsx ................ Настройки системы

components/
└── admin/
    ├── AdminNav.tsx .................... Навигация
    ├── shared/ ......................... Общие компоненты
    │   ├── DataTable.tsx
    │   ├── Pagination.tsx
    │   ├── SearchBar.tsx
    │   ├── StatusBadge.tsx
    │   ├── LoadingSpinner.tsx
    │   └── EmptyState.tsx
    ├── Dashboard/
    │   ├── MetricsGrid.tsx
    │   ├── RecentActivities.tsx
    │   └── SimpleChart.tsx
    ├── Finance/
    │   ├── FinanceMetricsGrid.tsx
    │   ├── RevenueChart.tsx
    │   └── PayoutsManager.tsx
    └── Settings/
        ├── SystemSettings.tsx
        └── EmailTemplatesManager.tsx

app/api/admin/
├── dashboard/
│   └── route.ts ........................ Dashboard данные
├── users/
│   ├── route.ts ........................ Список пользователей
│   └── [id]/
│       ├── route.ts .................... Детали пользователя
│       ├── block/
│       │   └── route.ts ................ Блокировка
│       └── unblock/
│           └── route.ts ................ Разблокировка
├── content/
│   ├── tours/
│   │   ├── route.ts .................... Список туров
│   │   └── [id]/
│   │       └── route.ts ................ Управление туром
│   ├── partners/
│   │   ├── route.ts .................... Список партнёров
│   │   └── [id]/
│   │       ├── route.ts ................ Управление партнёром
│   │       └── verify/
│   │           └── route.ts ............ Верификация
│   └── reviews/
│       ├── route.ts .................... Список отзывов
│       └── [id]/
│           └── moderate/
│               └── route.ts ............ Модерация отзыва
├── finance/
│   ├── metrics/
│   │   └── route.ts .................... Финансовые метрики
│   ├── revenue/
│   │   └── route.ts .................... График выручки
│   └── payouts/
│       ├── route.ts .................... Список выплат
│       └── [id]/
│           ├── approve/
│           │   └── route.ts ............ Одобрение выплаты
│           ├── reject/
│           │   └── route.ts ............ Отклонение выплаты
│           └── complete/
│               └── route.ts ............ Завершение выплаты
└── settings/
    ├── route.ts ........................ Настройки
    └── email-templates/
        ├── route.ts .................... Список шаблонов
        └── [id]/
            ├── route.ts ................ Управление шаблоном
            └── test/
                └── route.ts ............ Тестовая отправка
```

---

---

# 🚀 ЗАКЛЮЧЕНИЕ

## ✅ Что создано

**8 полноценных страниц** для администратора:
1. ✅ Страница входа (с режимами Login/Register)
2. ✅ Dashboard (метрики, графики, активность)
3. ✅ Управление пользователями (поиск, фильтры, блокировка)
4. ✅ Модерация туров (активация, архивация)
5. ✅ Модерация партнёров (верификация)
6. ✅ Модерация отзывов (одобрение, удаление)
7. ✅ Финансовое управление (обзор, выплаты)
8. ✅ Настройки системы (конфигурация, email шаблоны)

---

## 🎯 Ключевые особенности

- ✅ **Единый дизайн** — Samsung Weather тема на всех страницах
- ✅ **Полная защита** — компонент `<Protected roles={['admin']}>` на каждой странице
- ✅ **Удобная навигация** — липкая навигация `AdminNav` с подсветкой активной страницы
- ✅ **Модульная архитектура** — переиспользуемые компоненты (DataTable, SearchBar, Pagination)
- ✅ **REST API** — 15 endpoints для всех операций
- ✅ **Состояния загрузки** — Loading / Error / Success на каждой странице
- ✅ **Фильтры и поиск** — быстрый доступ к данным
- ✅ **Пагинация** — 20 записей на странице
- ✅ **Графики и аналитика** — визуализация данных

---

## 🔗 Связанные документы

- `📝_КАК_АДМИНИСТРАТОР_МОДЕРИРУЕТ.md` — подробное описание процесса модерации
- `📋_АНАЛИЗ_РОЛИ_АДМИНИСТРАТОРА.md` — анализ роли администратора
- `📊_ПОЛНАЯ_СХЕМА_И_ОПИСАНИЕ_ВСЕХ_СТРАНИЦ.md` — схема всех страниц проекта
- `📈_ВИЗУАЛЬНАЯ_СХЕМА_СТРАНИЦ.md` — Mermaid диаграммы

---

## 🎯 Итоговая карта: От входа до всех функций

```
🚪 Вход (/auth/login)
    ↓
🏠 Dashboard (/hub/admin)
    ↓
    ├─ 👥 Пользователи (/hub/admin/users)
    ├─ 🏔️ Туры (/hub/admin/content/tours)
    ├─ 🤝 Партнёры (/hub/admin/content/partners)
    ├─ 💬 Отзывы (/hub/admin/content/reviews)
    ├─ 💰 Финансы (/hub/admin/finance)
    │   ├─ 📊 Обзор
    │   └─ 💸 Выплаты
    └─ ⚙️ Настройки (/hub/admin/settings)
        ├─ 🛠️ Системные настройки
        └─ 📧 Email шаблоны
```

---

**Дата создания:** 27 ноября 2025  
**Статус:** ✅ Документация завершена на 100%  
**Сервер:** http://147.45.158.166
