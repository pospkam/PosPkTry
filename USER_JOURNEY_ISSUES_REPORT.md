# Отчет по недочетам: Анализ путей пользователей

**Дата:** 2025-11-10  
**Анализ:** Полный аудит по всем 6 ролям пользователей  
**Статус:** КРИТИЧНЫЕ НЕДОЧЕТЫ НАЙДЕНЫ

---

## 1. TOURIST (Турист)

### Критичные проблемы:
1. **Фильтры не работают** (`app/hub/tourist/page.tsx:164-206`)
   - Фильтры `activity`, `difficulty`, `priceRange` не применяются к отображаемым турам
   - `tours` массив отображается полностью без фильтрации
   - Нет функции `applyFilters()` или похожей логики

2. **Кнопка "Забронировать" не функциональна** (строка 263-265)
   ```typescript
   <button className="px-6 py-2 bg-premium-gold...">
     Забронировать
   </button>
   ```
   - Нет `onClick` обработчика
   - Нет перехода на страницу бронирования
   - Нет передачи `tourId`

3. **Дублирование вкладки "Transfers"** (строки 274-282 и 358-366)
   - Вкладка "transfers" реализована дважды
   - Один блок кода избыточен

4. **Избранное не реализовано** (строки 376-382)
   - Только пустая заглушка
   - Нет API endpoint `/api/favorites`
   - Нет хранения в localStorage или БД

5. **Остались эмодзи в коде** (строки 54-85, 100-102)
   ```typescript
   const icons: { [key: string]: string } = {
     hiking: '🥾',
     sightseeing: '○',
     wildlife: '🐻',
     ...
   ```

6. **Бронирования - нет авторизации** (`app/hub/tourist/bookings/page.tsx:9`)
   - `userId` не получается из контекста
   - API запрос `/api/bookings` без параметра userId
   - Кнопки "Подробнее", "Отменить", "Оставить отзыв" не имеют обработчиков

### Средние проблемы:
- Нет обработки ошибок fetch (try-catch без UI feedback)
- Нет пагинации туров
- Жестко закодированные координаты погоды
- Нет проверки наличия `data.data.tours` vs `data.data.data`

---

## 2. OPERATOR (Туроператор)

### Критичные проблемы:
1. **Нет проверки владельца тура** (`app/hub/operator/tours/page.tsx:63-77`)
   - Метод `handleToggleActive` не проверяет, принадлежит ли тур текущему оператору
   - Потенциальная уязвимость: оператор может изменять чужие туры

2. **Несоответствие формата ответа API** (`app/api/tours/route.ts:29-30`)
   - Frontend ожидает `data.data.tours`
   - API возвращает `{ success: true, data: { tours, pagination } }`
   - Но в Tourist dashboard используется `data.data.data`

3. **TODO в production коде** (`app/api/bookings/route.ts:77, 84, 91, 94`)
   ```typescript
   activity: 'hiking', // TODO: добавить в БД
   images: [], // TODO: добавить из tour_assets
   id: row.tourId, // TODO: исправить на operator_id
   ```

4. **Отсутствует endpoint** `/api/operator/dashboard`
   - Dashboard пытается загрузить данные с несуществующего endpoint
   - Нет обработки 404 ошибки

5. **Модальное окно деталей не реализовано** (строка 55-56)
   ```typescript
   // TODO: Открыть модальное окно с деталями
   console.log('Viewing booking details:', booking);
   ```

### Средние проблемы:
- Нет валидации на создание тура (минимальная цена, даты и т.д.)
- Отсутствует проверка прав на удаление тура
- Mock данные в случае ошибки БД могут ввести в заблуждение

---

## 3. GUIDE (Гид)

### Критичные проблемы:
1. **100% MOCK DATA** (`app/hub/guide/page.tsx:37-93`)
   - Все данные жестко закодированы (расписание, группы, доходы)
   - НЕТ API интеграции вообще
   - Кнопки не работают:
     - "Добавить доступность" (строка 159-161)
     - "Подробности" (строка 197-199)
     - "Группа" (строка 200-202)
     - "Отменить" (строка 203-205)
     - "Связаться с группой" (строка 225-227)
     - "Сохранить изменения" (строка 492-494)

2. **Остались эмодзи** (строки 33, 100, 124, 372, 378, 384)
   ```typescript
   { id: 'weather', name: 'Погода', icon: '🌤️' },
   { id: 'profile', name: 'Профиль', icon: '👤' },
   ```

3. **Нет реального профиля гида**
   - Форма профиля не отправляет данные
   - Нет endpoint `/api/guide/profile`
   - Значения по умолчанию захардкожены

4. **Нет системы уведомлений**
   - Экстренные контакты только отображаются
   - Нет функции "Связаться с группой"

### Средние проблемы:
- Рейтинг гида захардкожен (4.9)
- Нет календаря для добавления доступности
- Статистика доходов генерируется случайно (`Math.random()`)

---

## 4. TRANSFER OPERATOR (Транспортный оператор)

### Критичные проблемы:
1. **Минимальная функциональность** (`app/hub/transfer-operator/page.tsx`)
   - Только метрики и кнопки быстрых действий
   - Остальные страницы НЕ РЕАЛИЗОВАНЫ:
     - `/hub/transfer-operator/vehicles` - 404
     - `/hub/transfer-operator/drivers/page.tsx` - есть, но пустая
     - `/hub/transfer-operator/transfers` - 404
     - `/hub/transfer-operator/requests` - 404

2. **Ошибка в Protected роли** (строка 13)
   ```typescript
   <Protected roles={['transfer_operator']}>
   ```
   - Должно быть `'transfer-operator'` или `'transfer'`
   - Несоответствие с database schema (`role` enum)

3. **Остались эмодзи** (строки 69, 76)
   ```typescript
   <div className="text-3xl mb-2">👨‍ </div>
   <div className="text-3xl mb-2">🚐</div>
   ```

4. **Метрики не работают**
   - `TransferOperatorMetricsGrid` пытается загрузить данные
   - Нет API endpoint `/api/transfer-operator/metrics`

### Средние проблемы:
- Нет списка текущих заказов
- Нет управления водителями/транспортом inline
- Period selector не влияет на данные

---

## 5. AGENT (Агент)

### Критичные проблемы:
1. **Все компоненты - заглушки** (`app/hub/agent/page.tsx`)
   - `RecentClientsTable` - нет данных
   - `UpcomingBookingsTable` - нет данных
   - `AgentMetricsGrid` - нет данных
   - Все возвращают пустые состояния

2. **Кнопки быстрых действий не работают** (строки 66-93)
   - Просто `window.location.href` на несуществующие страницы:
     - `/hub/agent/clients` - 404
     - `/hub/agent/bookings` - 404
     - `/hub/agent/vouchers` - 404
     - `/hub/agent/commissions` - 404

3. **Нет API integration**
   - Нет endpoint `/api/agent/dashboard`
   - Нет endpoint `/api/agent/clients`
   - Нет endpoint `/api/agent/vouchers`

4. **Система ваучеров не реализована**
   - Только кнопка "Создать ваучер"
   - Нет функционала выдачи/использования ваучеров
   - Нет таблицы в БД для vouchers (есть в schema, но не используется)

### Средние проблемы:
- Нет CRM функционала
- Нет истории продаж
- Нет расчета комиссий

---

## 6. ADMIN (Администратор)

### Критичные проблемы:
1. **API endpoint не существует** (`app/hub/admin/page.tsx:27`)
   ```typescript
   const response = await fetch(`/api/admin/dashboard?period=${period}`);
   ```
   - Endpoint `/api/admin/dashboard` НЕ СОЗДАН
   - Всегда возвращает ошибку

2. **Остались эмодзи** (строка 86)
   ```typescript
   🔄 Обновить
   ```

3. **Кнопки быстрых действий не функциональны** (строки 183-198)
   - Нет `onClick` обработчиков
   - Только пустые button элементы

4. **Модерация контента не работает**
   - Страницы `/hub/admin/content/*` существуют
   - Но нет функционала approve/reject
   - Нет уведомлений модераторов

5. **Система настроек пуста** (`/hub/admin/settings/page.tsx`)
   - Только UI форма
   - Нет сохранения настроек
   - Нет endpoint `/api/admin/settings`

### Средние проблемы:
- Нет системы ролей и прав
- Нет логов действий администратора
- Нет backup системы

---

## 7. ОБЩИЕ ПРОБЛЕМЫ (Все роли)

### Критичные:
1. **Авторизация не работает полноценно**
   - Компонент `<Protected>` не блокирует доступ
   - `useAuth()` hook не проверяет реальную сессию
   - Нет middleware для проверки JWT токенов
   - Header `x-user-id` не устанавливается автоматически

2. **Database schema не синхронизирована**
   ```sql
   role VARCHAR(50) NOT NULL CHECK (role IN ('tourist', 'operator', 'guide', 'transfer', 'agent', 'admin'))
   ```
   - В коде используется `transfer_operator`
   - В БД только `transfer`
   - Несоответствие приведет к ошибкам INSERT

3. **Нет обработки ошибок сети**
   - Все `fetch()` вызовы без timeout
   - Нет retry логики
   - Нет offline detection

4. **Pagination не работает**
   - API возвращает pagination metadata
   - UI не использует `offset` и `hasMore`
   - Нет кнопок "Загрузить еще" / "Следующая страница"

### Средние:
1. **Эмодзи остались после удаления**
   - В Tourist, Guide, Transfer Operator, Admin
   - Нужен еще один проход Perl скрипта

2. **Mock данные в production**
   - API `/api/tours` возвращает mock при ошибке БД
   - Может ввести в заблуждение

3. **Нет i18n**
   - Все тексты на русском
   - Нет поддержки английского языка

---

## 8. API ENDPOINTS - Состояние

### Существуют и работают:
- ✅ `GET /api/tours` - работает
- ✅ `POST /api/tours` - работает
- ✅ `GET /api/bookings` - работает (с багами)
- ✅ `POST /api/bookings` - работает
- ✅ `GET /api/weather` - работает

### Не существуют (404):
- ❌ `/api/operator/dashboard`
- ❌ `/api/operator/tours` (GET with filters)
- ❌ `/api/guide/*`
- ❌ `/api/transfer-operator/*`
- ❌ `/api/agent/*`
- ❌ `/api/admin/dashboard`
- ❌ `/api/favorites`
- ❌ `/api/reviews` (POST)

### Частично реализованы:
- ⚠️ `/api/bookings/[id]/cancel` - есть, но не тестировалась
- ⚠️ `/api/notifications/*` - есть, но не используется

---

## 9. DATABASE - Недочеты

1. **Нет foreign key constraints в некоторых таблицах**
2. **Enum roles не соответствует коду**
3. **Отсутствуют индексы на:**
   - `bookings.date`
   - `tours.activity`
   - `tours.price_from`
4. **JSONB поля не валидируются** (equipment_included, images)

---

## 10. ПРИОРИТЕТНЫЙ СПИСОК ИСПРАВЛЕНИЙ

### Срочно (блокируют работу):
1. Создать недостающие API endpoints для всех ролей
2. Исправить роль `transfer_operator` → `transfer` в БД и коде
3. Реализовать реальную авторизацию с JWT
4. Подключить фильтры туров к отображению
5. Исправить Protected компонент

### Важно (влияют на UX):
1. Добавить обработчики всех кнопок
2. Реализовать систему избранного
3. Убрать все mock данные из Guide
4. Реализовать модальные окна деталей
5. Добавить pagination UI

### Желательно (улучшения):
1. Удалить оставшиеся эмодзи
2. Добавить error boundaries
3. Добавить offline support
4. Реализовать i18n
5. Добавить unit tests

---

## 11. МЕТРИКИ ГОТОВНОСТИ ПО РОЛЯМ

| Роль | Готовность | Критичных багов | Средних багов |
|------|-----------|----------------|---------------|
| Tourist | 60% | 6 | 4 |
| Operator | 70% | 5 | 3 |
| Guide | 30% | 4 | 3 |
| Transfer | 20% | 4 | 2 |
| Agent | 25% | 4 | 3 |
| Admin | 50% | 5 | 3 |

**Средняя готовность:** 42.5%

---

## 12. ЗАКЛЮЧЕНИЕ

Проект имеет **хорошую архитектуру**, но **критически недоработан** по функциональности.

### Положительное:
- ✅ Качественный UI/UX дизайн
- ✅ Правильная структура проекта
- ✅ TypeScript типизация
- ✅ Компонентный подход

### Отрицательное:
- ❌ 50%+ функционала не работает
- ❌ Mock данные вместо реальных API
- ❌ Отсутствует авторизация
- ❌ Кнопки без обработчиков

### Рекомендация:
**Требуется 2-3 недели активной разработки** для приведения в production-ready состояние.
