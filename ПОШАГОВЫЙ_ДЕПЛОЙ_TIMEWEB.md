# 🚀 ПОШАГОВЫЙ ДЕПЛОЙ KAMHUB НА TIMEWEB CLOUD

**Дата:** 6 ноября 2025  
**Платформа:** Timeweb Cloud (Россия)  
**Время деплоя:** 30-60 минут

---

## ✅ ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. **Аккаунт Timeweb Cloud**
```
Зарегистрируйтесь: https://timeweb.cloud/
Подтвердите email и телефон
Пополните баланс: минимум 500₽
```

### 2. **Локальные требования**
```
✅ Git установлен
✅ Docker установлен (опционально)
✅ SSH клиент
✅ Все файлы проекта закоммичены
```

---

## 📋 ШАГ 1: СОЗДАНИЕ VDS СЕРВЕРА (5 минут)

### В панели Timeweb Cloud:

1. **Перейти:** Облачные серверы → Создать сервер
2. **Выбрать конфигурацию:**
   ```
   Операционная система: Ubuntu 22.04 LTS
   Тариф: Cloud 2 (рекомендуется)
     - 2 vCPU
     - 4 GB RAM
     - 50 GB SSD NVMe
     - Стоимость: ~1,200₽/мес
   
   Опционально:
   - Выделенный IP адрес
   - Автобэкапы
   ```

3. **Создать сервер** — ждать 2-3 минуты

4. **Сохранить данные:**
   ```
   IP адрес сервера: 123.456.789.0
   Root пароль: (из email Timeweb)
   ```

---

## 📋 ШАГ 2: СОЗДАНИЕ POSTGRESQL БАЗЫ (5 минут)

### В панели Timeweb Cloud:

1. **Перейти:** Базы данных → Создать базу
2. **Выбрать:**
   ```
   СУБД: PostgreSQL 15
   Тариф: Cloud S1
     - 1 vCPU
     - 2 GB RAM
     - 10 GB SSD
     - Стоимость: ~600₽/мес
   
   Название БД: kamhub
   Имя пользователя: kamuser
   Пароль: (создайте надёжный)
   ```

3. **Создать базу** — ждать 3-5 минут

4. **Сохранить connection string:**
   ```
   Host: db-XXXXX.timeweb.cloud
   Port: 5432
   Database: kamhub
   User: kamuser
   Password: ваш_пароль
   
   DATABASE_URL=postgresql://kamuser:пароль@db-XXXXX.timeweb.cloud:5432/kamhub
   ```

---

## 📋 ШАГ 3: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ (2 минуты)

### Windows:
```bash
# Используйте PowerShell или WSL
ssh root@123.456.789.0
# Введите пароль из email
```

### Linux/Mac:
```bash
ssh root@123.456.789.0
```

---

## 📋 ШАГ 4: УСТАНОВКА DOCKER (5 минут)

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Устанавливаем Docker Compose
apt install docker-compose -y

# Проверяем установку
docker --version
docker-compose --version
```

---

## 📋 ШАГ 5: КЛОНИРОВАНИЕ ПРОЕКТА (3 минуты)

```bash
# Устанавливаем Git
apt install git -y

# Клонируем репозиторий
cd /var/www
git clone https://github.com/ваш-username/kamhub.git
cd kamhub

# Проверяем файлы
ls -la
```

---

## 📋 ШАГ 6: НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ (5 минут)

```bash
# Создаём .env файл
nano .env
```

**Вставьте следующее (замените значения на свои):**

```env
# ============================================
# БАЗА ДАННЫХ (Timeweb Cloud PostgreSQL)
# ============================================
DATABASE_URL=postgresql://kamuser:ваш_пароль@db-XXXXX.timeweb.cloud:5432/kamhub
DATABASE_SSL=true
DATABASE_MAX_CONNECTIONS=20

# ============================================
# БЕЗОПАСНОСТЬ
# ============================================
JWT_SECRET=ваш-супер-секретный-ключ-минимум-32-символа-сгенерируйте-случайно
JWT_EXPIRES_IN=7d
NEXTAUTH_SECRET=другой-секретный-ключ-для-nextauth
NEXTAUTH_URL=https://ваш-домен.ru

# ============================================
# ПРИЛОЖЕНИЕ
# ============================================
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://ваш-домен.ru
PORT=3000

# ============================================
# EMAIL (SMTP) - настройте свой почтовый сервер
# ============================================
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=noreply@ваш-домен.ru
SMTP_PASS=ваш_пароль_от_почты
SMTP_FROM=KamHub <noreply@ваш-домен.ru>

# ============================================
# CLOUDPAYMENTS (регистрация: cloudpayments.ru)
# ============================================
CLOUDPAYMENTS_PUBLIC_ID=pk_ваш_публичный_ключ
CLOUDPAYMENTS_API_SECRET=ваш_api_секрет
CLOUDPAYMENTS_WEBHOOK_SECRET=ваш_webhook_секрет

# ============================================
# AI ПРОВАЙДЕРЫ (опционально)
# ============================================
GROQ_API_KEY=gsk_ваш_ключ_groq
DEEPSEEK_API_KEY=sk-ваш_ключ_deepseek
OPENROUTER_API_KEY=sk-or-ваш_ключ_openrouter

# ============================================
# КАРТЫ (опционально)
# ============================================
YANDEX_MAPS_API_KEY=ваш_ключ_яндекс_карт

# ============================================
# МОНИТОРИНГ (опционально)
# ============================================
SENTRY_DSN=https://your-sentry-dsn
NEXT_PUBLIC_SENTRY_DSN=https://your-public-sentry-dsn
```

**Сохранить:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 📋 ШАГ 7: ПРИМЕНЕНИЕ БД СХЕМ (10 минут)

```bash
# Проверяем подключение к БД
export DATABASE_URL="postgresql://kamuser:пароль@db-XXXXX.timeweb.cloud:5432/kamhub"

# Применяем базовые схемы
apt install postgresql-client -y
psql "$DATABASE_URL" -f scripts/init-postgresql.sql

# Применяем новые схемы (все модули)
psql "$DATABASE_URL" -f scripts/apply-new-schemas.sql

# Проверяем таблицы
psql "$DATABASE_URL" -c "\dt"

# Должно быть 58+ таблиц!
```

---

## 📋 ШАГ 8: СБОРКА И ЗАПУСК DOCKER (10 минут)

### Вариант A: Docker Compose (рекомендуется)

```bash
# Запускаем всё сразу
docker-compose up -d

# Проверяем статус
docker-compose ps

# Смотрим логи
docker-compose logs -f app
```

### Вариант B: Docker без compose

```bash
# Собираем образ
docker build -t kamhub:latest .

# Запускаем контейнер
docker run -d \
  --name kamhub \
  -p 3000:3000 \
  --env-file .env \
  kamhub:latest

# Проверяем логи
docker logs -f kamhub
```

---

## 📋 ШАГ 9: НАСТРОЙКА NGINX (10 минут)

```bash
# Устанавливаем Nginx
apt install nginx -y

# Создаём конфигурацию
nano /etc/nginx/sites-available/kamhub
```

**Вставьте:**

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    # Перенаправление на HTTPS (после получения SSL)
    # return 301 https://$server_name$request_uri;

    # Временно проксируем на HTTP
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Upload размер
    client_max_body_size 10M;
}
```

**Активируем:**

```bash
# Создаём симлинк
ln -s /etc/nginx/sites-available/kamhub /etc/nginx/sites-enabled/

# Проверяем конфигурацию
nginx -t

# Перезапускаем Nginx
systemctl restart nginx
systemctl enable nginx
```

---

## 📋 ШАГ 10: НАСТРОЙКА ДОМЕНА (5 минут)

### В панели Timeweb Cloud:

1. **Перейти:** Домены → Добавить домен
2. **Ввести:** `ваш-домен.ru`
3. **DNS записи:**
   ```
   A запись:
   @ → IP вашего сервера (123.456.789.0)
   www → IP вашего сервера (123.456.789.0)
   ```

4. **Сохранить** — DNS обновится через 1-24 часа

---

## 📋 ШАГ 11: УСТАНОВКА SSL (5 минут)

```bash
# Устанавливаем Certbot
apt install certbot python3-certbot-nginx -y

# Получаем SSL сертификат
certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Вводите:
# Email: ваш@email.ru
# Agree to terms: Y
# Share email: N
# Redirect to HTTPS: Y (рекомендуется)

# Автопродление SSL
systemctl enable certbot.timer
```

---

## 📋 ШАГ 12: ПРОВЕРКА РАБОТЫ (5 минут)

```bash
# Проверяем статус всех сервисов
docker-compose ps

# Проверяем логи приложения
docker-compose logs app

# Проверяем БД
docker-compose logs postgres

# Тестируем API
curl http://localhost:3000/api/health

# Тестируем через домен
curl https://ваш-домен.ru/api/health
```

### Ожидаемый результат:
```json
{
  "status": "ok",
  "timestamp": "2025-11-06T...",
  "database": "connected"
}
```

---

## 📋 ШАГ 13: СОЗДАНИЕ ПЕРВОГО АДМИНА (5 минут)

```bash
# Подключаемся к БД
psql "$DATABASE_URL"

# Создаём админа
INSERT INTO users (id, email, name, role, created_at, updated_at)
VALUES (
  gen_random_uuid(),
  'admin@kamhub.ru',
  'Администратор',
  'admin',
  NOW(),
  NOW()
);

# Выходим
\q
```

---

## ✅ ГОТОВО! KAMHUB ЗАПУЩЕН!

### 🎯 ПРОВЕРЬТЕ:

1. **Главная страница:**
   ```
   https://ваш-домен.ru
   ```

2. **Admin Panel:**
   ```
   https://ваш-домен.ru/hub/admin
   ```

3. **API Health:**
   ```
   https://ваш-домен.ru/api/health
   ```

4. **Магазин сувениров:**
   ```
   https://ваш-домен.ru/shop
   ```

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

### Управление Docker:
```bash
# Остановить всё
docker-compose down

# Перезапустить
docker-compose restart

# Обновить код
git pull
docker-compose up -d --build

# Смотреть логи
docker-compose logs -f app

# Очистить всё и пересобрать
docker-compose down -v
docker-compose up -d --build
```

### Мониторинг:
```bash
# Использование ресурсов
docker stats

# Статус сервисов
systemctl status nginx
docker-compose ps

# Логи Nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

### Обновление проекта:
```bash
cd /var/www/kamhub
git pull origin main
docker-compose up -d --build
```

---

## 🚨 TROUBLESHOOTING

### Проблема 1: Не работает подключение к БД
```bash
# Проверить DATABASE_URL в .env
cat .env | grep DATABASE_URL

# Проверить доступность БД
psql "$DATABASE_URL" -c "SELECT 1"

# Проверить firewall Timeweb Cloud
# В панели: Базы данных → Безопасность → Добавить IP сервера
```

### Проблема 2: 502 Bad Gateway
```bash
# Проверить, запущен ли контейнер
docker-compose ps

# Перезапустить приложение
docker-compose restart app

# Проверить логи
docker-compose logs app
```

### Проблема 3: SSL не работает
```bash
# Проверить DNS
nslookup ваш-домен.ru

# Переустановить SSL
certbot --nginx -d ваш-домен.ru --force-renewal
```

---

## 📊 МОНИТОРИНГ И БЭКАПЫ

### Настройка мониторинга:
```bash
# Установка monitoring stack (опционально)
docker-compose -f docker-compose.monitoring.yml up -d
```

### Автоматические бэкапы БД:
```bash
# Создать скрипт бэкапа
nano /root/backup-db.sh
```

```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/var/backups/kamhub"
mkdir -p $BACKUP_DIR

# Бэкап БД
pg_dump "$DATABASE_URL" > $BACKUP_DIR/kamhub_$DATE.sql

# Удалить старые (>7 дней)
find $BACKUP_DIR -name "*.sql" -mtime +7 -delete

echo "✅ Backup completed: kamhub_$DATE.sql"
```

```bash
# Сделать исполняемым
chmod +x /root/backup-db.sh

# Добавить в cron (каждый день в 3:00)
crontab -e
# Добавить строку:
0 3 * * * /root/backup-db.sh >> /var/log/kamhub-backup.log 2>&1
```

---

## 🎯 ФИНАЛЬНАЯ ПРОВЕРКА

### ✅ ЧЕК-ЛИСТ ЗАПУСКА:

- [ ] VDS сервер создан и доступен
- [ ] PostgreSQL БД создана
- [ ] .env файл настроен
- [ ] БД схемы применены (58+ таблиц)
- [ ] Docker контейнеры запущены
- [ ] Nginx настроен
- [ ] Домен привязан к серверу
- [ ] SSL сертификат получен
- [ ] API /health возвращает OK
- [ ] Админ пользователь создан
- [ ] Главная страница открывается
- [ ] Admin Panel доступен

---

## 🎉 ПОЗДРАВЛЯЮ!

**KamHub успешно развёрнут на Timeweb Cloud!** 🎊

### 📈 ЧТО ДАЛЬШЕ:

1. **Добавить контент:**
   - Туры в базу данных
   - Сувениры от "Дар Севера"
   - Снаряжение для проката
   - Автомобили в каталог

2. **Пригласить партнёров:**
   - Туроператоры
   - Агенты
   - Транспортные компании

3. **Маркетинг:**
   - SEO оптимизация
   - Социальные сети
   - Реклама в Яндекс/Google

4. **Мониторинг:**
   - Настроить аналитику
   - Отслеживать метрики
   - Собирать обратную связь

---

## 🏔️ KAMHUB ЗАПУЩЕН И ГОТОВ К РАБОТЕ!

**Стоимость инфраструктуры:**
```
VDS Cloud 2:      ~1,200₽/мес
PostgreSQL Cloud: ~600₽/мес
Домен (.ru):      ~299₽/год
SSL:              Бесплатно (Let's Encrypt)

ИТОГО: ~1,800₽/мес (~$20/мес)
```

**Возможности:**
- ⚡ Высокая скорость (SSD NVMe)
- 🔒 Enterprise безопасность
- 📈 Масштабируемость
- 🇷🇺 Серверы в России
- 💬 Поддержка 24/7 на русском

---

**Отчёт подготовлен:** 6 ноября 2025  
**Статус деплоя:** ✅ **ГОТОВ К ЗАПУСКУ**  
**Время:** 30-60 минут  
**Сложность:** ⭐⭐⭐☆☆ (Средняя)

🚀 **УДАЧНОГО ЗАПУСКА!** 🚀

