# Pillar-Cluster Архитектура: План Миграции

## 📋 Обзор

Проект KamchatourHub переходит на архитектуру на основе Pillar-Cluster для улучшения масштабируемости, модульности и командной разработки.

## 🏗️ Структура Pillars

```
pillars/
├── core-infrastructure/
│   ├── api/              # API endpoints для ядра системы
│   ├── lib/              # Общие утилиты (auth, db, cache, monitoring)
│   └── types/            # Типы и интерфейсы для всей системы
├── discovery/
│   ├── api/              # API маршруты для поиска
│   ├── components/       # React компоненты для поиска
│   ├── lib/              # Логика туров, размещений, транспорта
│   └── types/            # Типы для Discovery
├── booking/
│   ├── api/              # API для бронирования и платежей
│   ├── components/       # Компоненты для бронирования
│   ├── lib/              # Логика бронирования, корзины
│   └── types/            # Типы для Booking
├── engagement/
│   ├── api/              # API для отзывов, уведомлений
│   ├── components/       # Компоненты для взаимодействия
│   ├── lib/              # Логика лояльности, чатов
│   └── types/            # Типы для Engagement
└── partner-management/
    ├── api/              # API для администрирования
    ├── components/       # Компоненты панелей управления
    ├── lib/              # Логика ролей и прав доступа
    └── types/            # Типы для Partner Management
```

## 🎯 Pillar Описания

### 1. Core Infrastructure
**Назначение:** Ядро системы, общая функциональность

**Модули:**
- `auth/` - Аутентификация и авторизация
- `database/` - Подключение к БД, миграции
- `cache/` - Кэширование (Redis)
- `monitoring/` - Логирование и мониторинг
- `notifications/` - Система уведомлений
- `payments/` - Интеграции платежей
- `config/` - Конфигурация приложения

**Зависимости:** Нет зависимостей от других Pillars

---

### 2. Discovery
**Назначение:** Поиск и обнаружение туристических продуктов

**Модули:**
- Tours API - Список, фильтры, детали туров
- Accommodations API - Размещения (отели, хостелы)
- Transport API - Трансферты, такси
- Weather API - Интеграция с сервисами погоды
- Gear API - Спортснаряжение и экипировка
- Search Components - Интерфейсы поиска

**Зависимости:** 
- ✅ Core Infrastructure (auth, db, cache)
- ❌ Другие Pillars напрямую не используют

**API Контракты:**
- GET `/api/discovery/tours?filters=...`
- GET `/api/discovery/accommodations/:id/availability`
- GET `/api/discovery/transport/:id/schedule`

---

### 3. Booking
**Назначение:** Бронирование и покупки

**Модули:**
- Cart API - Управление корзиной
- Booking API - Создание и управление бронированиями
- Payments API - Обработка платежей
- Reservations - Управление резервами

**Зависимости:**
- ✅ Core Infrastructure (auth, db, payments)
- ✅ Discovery (только через API, event-driven)

**Event-Driven Интеграция:**
```
Discovery -> Event: product.checked_availability
Booking   -> Listen: product.checked_availability
Booking   -> Event: booking.created
Discovery -> Listen: booking.created (обновить доступность)
```

---

### 4. Engagement
**Назначение:** Взаимодействие и лояльность

**Модули:**
- Reviews API - Система отзывов
- Loyalty API - Программа лояльности и баллы
- Chat API - Чат с поддержкой
- Notifications API - Отправка уведомлений

**Зависимости:**
- ✅ Core Infrastructure (auth, db, notifications)
- ✅ Discovery (через API, для рекомендаций)
- ✅ Booking (через API, для истории покупок)

---

### 5. Partner Management
**Назначение:** Администрирование и управление партнёрами

**Модули:**
- Admin Panel API - Администраторы системы
- Operator Panel API - Операторы туров
- Agent Panel API - Агенты и гиды
- Guide Management - Управление гидами
- Role Management - Управление ролями и правами

**Зависимости:**
- ✅ Core Infrastructure (auth, db, monitoring)
- ✅ Все остальные Pillars (для управления и аналитики)

---

## 🔄 Этапы Миграции

### ✅ Этап 1: Создание базовой структуры
- [x] Создать директории для каждого Pillar
- [x] Создать поддиректории (api, components, lib, types)
- [x] Обновить tsconfig.json с aliases

### 📅 Этап 2: Миграция Core Infrastructure
**Приоритет:** ВЫСОКИЙ (блокирует другие этапы)

- [ ] Перенести `lib/auth/` → `pillars/core-infrastructure/lib/auth/`
- [ ] Перенести `lib/database/` → `pillars/core-infrastructure/lib/database/`
- [ ] Перенести `lib/cache.ts` → `pillars/core-infrastructure/lib/cache/`
- [ ] Перенести `lib/monitoring/` → `pillars/core-infrastructure/lib/monitoring/`
- [ ] Перенести `lib/notifications/` → `pillars/core-infrastructure/lib/notifications/`
- [ ] Перенести `lib/payments/` → `pillars/core-infrastructure/lib/payments/`
- [ ] Обновить импорты во всех файлах проекта
- [ ] Создать `index.ts` для переэкспорта общедоступного API

### 📅 Этап 3: Миграция Discovery
**Приоритет:** ВЫСОКИЙ

- [ ] Перенести `app/api/tours/` → `pillars/discovery/api/`
- [ ] Перенести `app/api/accommodations/` → `pillars/discovery/api/`
- [ ] Перенести `lib/weather/` → `pillars/discovery/lib/weather/`
- [ ] Перенести компоненты поиска в `pillars/discovery/components/`
- [ ] Убедиться, что нет импортов из других Pillars

### 📅 Этап 4: Миграция Booking
**Приоритет:** ВЫСОКИЙ

- [ ] Перенести `app/api/cart/` → `pillars/booking/api/`
- [ ] Перенести `app/api/tours/[id]/book/` → `pillars/booking/api/`
- [ ] Перенести компоненты бронирования
- [ ] Настроить event-driven интеграцию с Discovery

### 📅 Этап 5: Миграция Engagement
**Приоритет:** СРЕДНИЙ

- [ ] Перенести `app/api/reviews/` → `pillars/engagement/api/`
- [ ] Перенести `lib/loyalty/` → `pillars/engagement/lib/`
- [ ] Перенести компоненты отзывов и лояльности

### 📅 Этап 6: Миграция Partner Management
**Приоритет:** СРЕДНИЙ

- [ ] Перенести панели администратора
- [ ] Перенести панели операторов
- [ ] Перенести логику ролей

### 📅 Этап 7: Event Bus & API Gateway
**Приоритет:** СРЕДНИЙ

- [ ] Реализовать EventBus для межпилларного общения
- [ ] Создать API Gateway для маршрутизации
- [ ] Документировать события и контракты

### 📅 Этап 8: Тестирование и рефакторинг
**Приоритет:** ВЫСОКИЙ

- [ ] Написать интеграционные тесты между Pillars
- [ ] Проверить отсутствие circular dependencies
- [ ] Рефакторинг циклических зависимостей
- [ ] Полное тестирование на production

---

## 📐 Правила Архитектуры

### 1. Правило Зависимостей
```
Partner Management
├── может использовать все
└── используется никем

Engagement
├── может использовать: Core, Discovery, Booking
└── используется: Partner Management

Booking
├── может использовать: Core, Discovery
└── используется: Engagement, Partner Management

Discovery
├── может использовать: Core
└── используется: Booking, Engagement, Partner Management

Core Infrastructure
├── может использовать: никакие
└── используется: все остальные
```

### 2. Правило Импортов
```typescript
// ✅ ПРАВИЛЬНО: Импорты из Core Infrastructure
import { getUser, checkAuth } from '@core-infrastructure/lib/auth';
import { getDatabase } from '@core-infrastructure/lib/database';

// ✅ ПРАВИЛЬНО: Импорты внутри Pillar
import { SearchFilter } from '@discovery/lib/filters';
import { TourCard } from '@discovery/components';

// ❌ НЕПРАВИЛЬНО: Импорты между Pillars (кроме через API/Events)
import { getUserBookings } from '@booking/lib/bookings'; // discovery не должен знать о booking

// ❌ НЕПРАВИЛЬНО: Импорты из других слоёв
import { bookingComponent } from '../../components/booking'; // использовать aliases
```

### 3. Правило Общения между Pillars
```typescript
// API Method:
const response = await fetch('/api/discovery/tours/1/availability');

// Event Method:
import { eventBus } from '@core-infrastructure/lib/events';
eventBus.emit('booking:created', { bookingId, tourId });
eventBus.on('discovery:tour_updated', (data) => { /* handle */ });
```

### 4. Правило Типов
- Типы, используемые несколькими Pillars → `@core-infrastructure/types/`
- Типы для одного Pillar → в папку `types/` этого Pillar

---

## 🚀 Команды для Миграции

```bash
# Проверить статус миграции
npm run pillar:check

# Проверить зависимости между Pillars
npm run pillar:deps

# Запустить linter для проверки импортов
npm run pillar:lint

# Провести рефакторинг импортов
npm run pillar:refactor

# Запустить тесты
npm run test:pillars
```

---

## 📊 Прогресс Миграции

| Этап | Статус | Основное | 
|------|--------|---------|
| 1. Базовая структура | ✅ | Завершено |
| 2. Core Infrastructure | 📋 | В очереди |
| 3. Discovery | 📋 | В очереди |
| 4. Booking | 📋 | В очереди |
| 5. Engagement | 📋 | В очереди |
| 6. Partner Management | 📋 | В очереди |
| 7. Event Bus & API Gateway | 📋 | В очереди |
| 8. Тестирование | 📋 | В очереди |

---

## 📚 Ссылки на Документацию

- [Pillar Cluster Architecture](./PILLAR_CLUSTER_ARCHITECTURE.md)
- [Enforcement Rules](./PILLAR_CLUSTER_ENFORCEMENT.md)
- [Implementation Guide](./PILLAR_CLUSTER_IMPLEMENTATION_GUIDE.md)
- [Quick Reference](./PILLAR_CLUSTER_QUICK_REF.md)
