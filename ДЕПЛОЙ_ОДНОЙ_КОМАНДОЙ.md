# 🚀 ДЕПЛОЙ ОДНОЙ КОМАНДОЙ

## Скопируйте и выполните на сервере:

```bash
ssh root@147.45.158.166 'cd /var/www/kamchatour && git fetch origin main && git reset --hard origin/main && git checkout origin/main -- deploy-147-45-158-166.sh && chmod +x deploy-147-45-158-166.sh && ./deploy-147-45-158-166.sh'
```

## Или подключитесь и выполните:

```bash
# 1. Подключение
ssh root@147.45.158.166

# 2. Деплой одной командой
cd /var/www/kamchatour && \
git fetch origin main && \
git reset --hard origin/main && \
npm install && \
pm2 delete kamchatour; \
rm -rf .next && \
npm run build && \
pm2 start npm --name kamchatour -- start && \
pm2 save && \
pm2 logs kamchatour --lines 20
```

**Готово!** 🎉
